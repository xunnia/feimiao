# 预算与资产管理 UX / 数据方案 V2.1（口径锁定版）

> 状态：V2.1 锁定方案（代码 / DB 尚未实施）。已纳入逻辑、经济学、用户体验和统计口径复盘；结构性 UI / DB 实施必须同时遵守 `STATISTICS_CALCULATION_STANDARD.md`。
> 基线：`1.186.0+188 / DB v32`。
> 版本关系：`BUDGET_ASSET_UX_PLAN_V2_2026-07-12.md` 保留为竞品研究稿；本文在 V2 上修正双日期与到账账户质量、固定支出互斥预留、校准锚点、预算修订、物品成本、权益归档迁移、快照可信度和净资产算式。
> UI 继续服从 `docs/claude/UI_DESIGN_STANDARD.md`：不使用红色，绿色只允许低饱和预算健康态 `#7FB069`，不增加底部 Tab，按钮、表单、主题和文字层级沿用现有标准。

## 0. 结论先行

### 0.1 有数到底做得怎么样

**有数在“物品资产”这个细分方向里做得很好，但它不是完整个人资产管理的答案。**

它做好的不是银行账户、信用卡、贷款和投资组合核对，而是把“买过的东西”做成一个有照片、有陪伴时间、有日均花费、有目标进度、有退出结算的数字陈列柜。它回答的是：

- 我拥有什么？
- 这件东西用了多久？
- 买得值不值？
- 是继续用、闲置、卖掉，还是退役？

它最强的留存机制是：**用户录入一次，日均花费会随着时间自动下降，系统持续产生可回看的变化。**传统记账反复告诉用户“又花了多少钱”，有数会告诉用户“这台手机已经降到每天 8 元”。这种变化比静态净资产表更容易让用户回来，但它不自动证明“确实用值了”：物品即使一直闲置，分母也会变大，因此肥喵不能只因时间过去而庆祝或给价值判断。

但是它的日均成本和总资产净值不能直接当作严谨财务口径：

- `购入价 / 持有天数` 即使物品一直吃灰也会下降，所以它不是利用率。
- 普通物品的二手价值依赖主观估值，长期不更新会失真。
- 物品购入价、当前估值、持有成本和财务净资产是四个概念，不能混成一个数字。

因此，肥喵应学习有数的**照片陈列、低维护反馈和生命周期闭环**，不能拿有数的页面直接替换整个资产管理。

### 0.2 新版产品骨架

资产管理固定为一个入口、三个页面级视图：

```text
资产管理                                      [＋]
[ 总览 ] [ 资金 ] [ 物品 ]

总览：我现在整体有多少，数字是否可信，有什么待处理
资金：账户、投资余额、权益、负债和余额校准
物品：照片陈列、日均持有花费、保值率和生命周期
```

底层可以关联，用户心智必须分开：

1. **钱的资产**：账户余额、投资余额、权益、负债、净资产、校准和趋势。
2. **拥有的东西**：实物照片、购买账单、持有时间、使用状态、出售和退货。

预算继续是独立域：

- 预算回答“这段时间的钱准备怎么花、还能花多少”。
- 资产回答“钱和价值现在在哪里、发生了什么变化”。
- 同一笔购买可以同时进入预算支出并生成一个物品，但不能重复计算或互相抵消。

### 0.3 V2.1 相比 V2 的关键修正

1. 保留“总览 / 资金 / 物品”三视图，不推翻已经成立的产品骨架。
2. 一笔经济事件允许同时具有消费归属日 `attribution_date` 和账户结算日 `settled_at`；退款不再用一个日期兼顾两套互相冲突的统计。
3. 余额校准改为绝对 `target_balance` 锚点；差额只用于保存前解释，不作为后续余额真相。
4. 预算浏览固定区分“本周期 / 自然月 / 自然周 / 自定义”，任何按钮不再随计划类型改变语义。
5. 本周期改预算区分“把周期总额改为 X”和“在剩余期间追加/减少 Y”，最终都保存为本周期绝对目标额，禁止拼接新旧日均额度反推周期总额。
6. 同一账本同一日期最多一个日常主计划；专项明确改名为“专项追踪”，可以重叠观察但绝不能与主计划相加。
7. 物品统一使用净购置成本、累计持有投入、可变现净估值和最终净成本四个基数，退款只扣一次。
8. 归档只改变展示可见性；出售、退货、报废、丢失、赠送、结清才改变经济状态和计入净资产范围。
9. 自动快照是可重算的 `computed_snapshot`；只有明确核对且记录覆盖范围的结果才是 `verified_checkpoint`。
10. 修正总资产、资金净值、总负债和净资产的集合与算术，所有组成项必须互斥且可下钻。
11. “今日可用”只在能识别尚未发生的固定支出时给出可自由安排额；否则降级为诚实的日均参考，不冒充安全现金余额。
12. 物品日均持有花费是时间摊薄指标，不代表利用率或收益，不因单纯时间流逝触发“更值了”的庆祝。
13. 非 CNY、未知估值、缺少校准等情况显示为“未计入 / 不可计算”，绝不静默当作 0。
14. 主页、预算、统计、Widget、月报、AI 洞察和快速记账统一依赖专门的统计口径标准与 resolver，不再各自解释退款、笔数和同期比较。

---

## 1. 研究方法与证据边界

本次研究使用三类证据：

1. Apple App Store 官方产品描述、版本说明、截图和 Apple 托管评论。
2. 产品官方帮助中心、官方 FAQ 和官方更新日志。
3. 肥喵当前代码、数据库 schema、Repository 行为和测试。

本文将结论分为：

- **FACT**：官方资料或代码直接支持的事实。
- **INFERENCE**：根据产品结构、评论和使用成本作出的产品判断。
- **RECOMMENDATION**：结合肥喵定位给出的取舍，不冒充竞品事实。

限制：

- App Store 可见评论由 Apple 选择和排序，不能代表完整用户样本。
- 有数可见评论有重复文案，不能把每条都当作独立证据。
- 评分数不等于下载量、活跃用户数或留存率。
- 本次没有安装所有竞品，未把未验证的具体交互和计算公式写成事实。
- Bing 搜索出现验证码，搜索摘要没有被用作证据。
- Apple 旧 RSS 评论接口返回成功但没有评论条目；网页评论接口直接调用受限，本文只引用 App Store 页面可见内容。

---

## 2. 竞品深度结论

## 2.1 物品资产型

截至 2026-07-12，中国区 App Store 数据：

| App | 核心定位 | 评分 / 评分数 | 最值得学 | 主要维护风险 |
|---|---|---:|---|---|
| 有数 | 数字陈列、日均成本、卖出复盘 | 4.6 / 3923 | 照片优先、被动正反馈、目标成本 | 估值口径不透明；日均成本不等于利用率 |
| 持物 | 结构化资产台账、维护与估值 | 4.8 / 323 | 维护费用、结构图、批量管理、本地优先 | 容易向单位、库存、贵金属行情扩张 |
| 知我记物 | 收藏展示、提醒、回顾 | 4.5 / 50 | 多种录入、保修/保质期、回顾卡片 | 字段和视图过多，物品多后性能与筛选压力大 |
| 与物 | 日均/次均成本、极简物品管理 | 4.7 / 33 | 功能克制、附加费用、退出成本 | 次均成本需要持续打卡；样本量较小 |

### 有数：为什么打中用户

FACT：官方主轴集中在“目标日均成本、服役时间、持有成本、卖出盈亏、AI 抠图和数字陈列柜”。官方截图的首屏也是照片网格，不是账户表格；每件物品直接显示购入价、持有天数、日均花费和目标进度。

FACT：截至访问日，有数中国区约 3923 个评分，约为持物、知我记物、与物三者评分数之和的 9.66 倍。它只能说明市场吸引力明显领先，不能说明真实留存。

INFERENCE：有数的核心循环是：

```text
拍照/录入一次
    ↓
每天自动增加持有天数
    ↓
日均花费下降、目标进度上升
    ↓
用户得到“用得越来越值”的反馈
    ↓
继续使用 / 决定卖出 / 下次购买前回看
```

这个循环的价值在于“反馈自动发生”，而不是要求用户天天维护。

### 物品类产品共同的五个任务

| 用户任务 | 真正要回答的问题 | 推荐频率模型 |
|---|---|---|
| 盘点 | 我到底拥有什么，放在哪，有没有重复买 | 初次高峰，之后低频 |
| 判断值不值 | 用了多久，每天或每次花了多少 | 高频查看，低频录入 |
| 保管 | 发票、保修、保养何时到期 | 低频但高价值 |
| 购买前决策 | 手里的东西还没用够本，要不要再买 | 购买前偶发 |
| 退出复盘 | 退货、卖掉、赠送、报废后实际花了多少 | 低频但形成闭环 |

### 已暴露的真实痛点

- 有数用户要求套装部分卖出、购买平台字段；这会迅速引出组件、分摊成本和部分处置，首版不应贸然做。
- 持物用户认为逐次记录使用次数太复杂，更希望“常用 / 偶尔 / 闲置”这类低维护表达。
- 知我记物用户在数百件物品后遇到性能、筛选和信息密度问题，说明陈列柜必须懒加载、搜索和筛选先于更多视图。
- 与物用户指出附加费用在不同页面口径不一致，说明购入成本、附加成本、估值和出售收入必须共用一套公式。
- 数据安全是物品类的信任底线，照片若不能完整备份，功能越精美风险越大。

## 2.2 综合资产和预算型

| App | 真正强项 | 核心任务 | 维护负担 | 对肥喵的价值 |
|---|---|---|---|---|
| 钱往 | 国内记账 + 股票基金 + 债务 + 净资产 | 快速记账、看现金流和投资盈亏 | 中 | 学资产聚合和投资/日常并存，避免长页面和行情依赖 |
| MoneyWiz | 专业账户台账、核对、贷款、投资、多币种 | 导入、核对、预算、贷款拆分 | 中高 | 学账户详情、校准/核对和归档，不照搬专业 ERP 复杂度 |
| Monarch | 自动聚合、家庭协作、月度规划 | 审核新流水、看净资产、做月预算 | 低中 | 学总览和待审核；国内首版不做银行连接 |
| YNAB | 主动消费决策和零基预算方法 | 每日调整计划、每周核对、月末复盘 | 高且有意为之 | 学“账户是位置、预算是用途”，不强迫肥喵用户执行整套仪式 |
| Actual Budget | 本地优先、信封预算、off-budget 资产 | 手动同步、核对、只分配已有现金 | 高 | 学 on/off-budget 边界和可审计核对，不学自托管 UX |
| Cashew | 月/周/自定义预算和清晰图表 | 手动记账、看周期进度 | 中低 | 证明浏览周期与计划周期应该解耦 |

### 钱往

FACT：钱往把快速记账、预算、账户净资产、股票基金行情、每日盈亏、债务和多币种放在同一产品里。其可见评论中，“多资产管理”和“兼顾记账与投资”直接影响付费意愿。

INFERENCE：它的优势是覆盖完整，风险也是覆盖完整。评论同时出现股票菜单对非投资用户无关、投资列表过长、希望截图导入、信用卡分期复杂等问题。肥喵应学习“资金资产和日常账单底层相连”，不应立即复制证券行情和长持仓页面。

### MoneyWiz

FACT：MoneyWiz 支持手动账户、银行连接、简单/高级核对、余额调整、贷款本金和利息拆分、投资与多币种。高级 reconciliation 会要求期初、期末、日期和逐笔状态。

INFERENCE：它适合专业用户，但把这套完整心智放进肥喵会伤害“可爱、简单、好上手”的定位。最值得借鉴的是：账户必须有详情、历史、归档和明确核对入口。

### Monarch

FACT：Monarch 的手工账户官方建议用于车辆、珠宝、房产等“变化不频繁”的对象；月度预算与账户余额分工明确；新同步交易有 Needs Review；Goals 把真实余额进度和预算计划贡献分开。

INFERENCE：资产更新本来就应该低频，首屏应帮助用户发现“哪里需要处理”，而不是鼓励天天编辑估值。肥喵可以学待审核和目标双口径，不做银行连接首版。

### YNAB

FACT：YNAB 官方区分 daily、weekly/payday、turn-of-month 和 monthly/quarterly tracking routine。它明确说明“账户表示钱在哪里，预算分类表示钱要做什么”，Tracking accounts 只进入净资产、不进入预算，并反复提醒 Tracking 会增加视觉、心智和维护负担。

INFERENCE：预算的高频价值是做消费决定，资产的价值是低频确认位置和变化。两个域共享数据，但不能共用一个操作模型。

---

## 3. 用户真正反复使用的核心功能

频率不是为了堆入口，而是决定首屏优先级。

| 频率 | 用户问题 | 产品应提供 | 不应强迫 |
|---|---|---|---|
| 每天 / 每周 | 还能花多少？最近有没有记错？余额是否异常？ | 有条件的可自由安排额/预算日均参考、最近账单、待审核 | 每天更新房产/物品估值 |
| 每周 / 每月 | 净资产和负债怎么变了？该还什么？ | 全局净资产、流动资产、待还款、核对覆盖和估算趋势 | 逐笔专业 reconciliation |
| 每月 / 季度 | 手工账户是否需要校准？投资/物品估值是否过期？ | 轻量校准、估值日期、复核提醒 | 强制统一复核周期 |
| 购买发生时 | 这笔耐用品要不要变成物品记录？ | 从账单两步加入、自动带入金额日期 | 重填一整套资料 |
| 生命周期事件 | 退货、卖出、收回、报废后怎么算？ | 关联原账单和账户、完整事件链、可撤销 | 另造互不相认的记录 |
| 低频复盘 | 哪些东西最值、哪些长期闲置？ | 日均持有花费、保值率、状态和筛选 | 复杂库存和十几种图表 |

产品原则：

1. 高频页面负责“看懂和发现问题”。
2. 中频操作负责“快速校准和确认”。
3. 低频详情负责“完整生命周期”，不挤进列表行。
4. 一次录入后能自动产生的反馈优先于需要持续打卡的指标。
5. 任何资产数字都要能追到来源、日期和事件。

---

## 4. 明确借鉴与明确不借鉴

## 4.1 从有数和物品类产品借鉴

- 照片是物品首要识别，不是表单最后一个附件路径。
- 宽度允许时两列陈列比银行式 ListTile 更适合物品；窄屏和大字体自动单列。
- 首屏直接给持有天数、日均持有花费和状态。
- 目标日均花费可以提供，但属于行为反馈，不进入净资产公式。
- 退货、出售、报废、丢失和赠送必须有经济终止状态和最终复盘；归档只是展示隐藏，不属于结束状态。
- 从账单、照片和订单降低录入成本，比增加更多字段更重要。
- 没有待处理事项时整个区块消失，不用空卡占位。

## 4.2 不借鉴

- 不把购入价、当前估值、日均花费和净资产混成一个“总资产”。
- 不把“持有天数长”说成“使用频率高”。
- 不默认要求每件物品逐次打卡。
- 不在首版做套装拆分、部分出售、复数库存和单位换算。
- 不把 AI 抠图设为必经步骤；它是录入和展示增强，不是数据地基。
- 不做贵金属、外币、股票行情和自动估值首版。
- 不先做分享海报、贴纸墙和社交玩法。
- 不用高饱和荧光绿照搬视觉；继续服从肥喵六主题和颜色铁律。

## 4.3 从综合财务产品借鉴

- 账户有详情、近期活动、校准日期和归档。
- 顶部同时回答净资产、流动资产、负债和可信变化。
- 预算和账户分工：账户是钱的位置，预算是钱的用途。
- 手工资产显示“上次核对时间”，不要求每日更新。
- 新导入/AI 结果进入待审核，比一开始上 cleared/locked 更适合肥喵。
- 存钱目标和债务目标的实际进度来自真实余额；预算只表达本周期计划贡献。

## 4.4 不借鉴综合产品的重能力

- 银行自动聚合首版。
- MoneyWiz 全套贷款摊销、证券交易和多预算信封转移。
- YNAB 强制零基预算与每日核账仪式。
- Monarch 多预算模式和复杂 Goals 分配引擎首版。
- Actual 的自托管、密钥和手动银行连接 UX。
- 静默修改历史余额，或把校准差额伪装成普通收入/支出。

---

## 5. 统一产品架构

## 5.1 三层关系

```text
账单流水（发生了什么）
  ├─ 预算：这笔支出是否在计划内、还剩多少
  ├─ 资金：哪个账户减少、哪个负债变化
  └─ 物品：是否形成一个可持续使用和处置的东西

预算 = 流量计划
资金/物品 = 存量状态
账单/事件 = 两者之间的桥
```

同一事件可以被不同视图观察，但不能只靠一个日期或一个金额字段承担全部语义：

| 统计轴 | 回答的问题 | 主时间 | 典型事件 |
|---|---|---|---|
| 消费统计 | 这段时间实际消费了什么？ | `attribution_date` | 普通支出、附着原单的退款净额 |
| 账户现金流 | 钱在什么时候真正进出账户？ | `settled_at` | 支付、退款到账、转账、贷款还款、出售收款 |
| 预算执行 | 哪个计划周期被占用了多少？ | `attribution_date` + 计划周期 | 计入预算的净支出 |
| 资产状态 | 某时点拥有多少价值和负债？ | 事件发生时间 / 估值时间 / 校准锚点 | 余额、估值、权益、负债、处置 |

普通账单的两个日期通常相同；跨期退款、延迟到账、预授权转正式扣款等场景可以不同。任何页面都不能各算一套公式，具体合同见 `STATISTICS_CALCULATION_STANDARD.md`。

## 5.2 资产范围

- 资产总览固定全局，不随抽屉当前账本变化。
- 资金账户和负债是全局对象。
- 实物、权益保留所属账本标签，可筛选，但筛选不改变顶部全局总额。
- 非 CNY 没有汇率时分币种展示，不静默相加；顶部必须写“人民币口径，未含 N 项外币资产/负债”，不能仍叫无条件完整净资产。
- 存量 `include_in_net_worth` 原样保留。
- 新增普通数码、家电、工具时默认不强迫计入净资产；房产、车辆、贵重物品、收藏品可推荐开启，但最终由用户确认。
- 归档只改变列表可见性，不得静默关闭 `include_in_net_worth`。非零账户、未结权益和未结负债即使归档，仍按经济状态计入合计。
- 多人共享账本上线后，资产总览默认仍是当前用户的私有全局域；共享成员只能看到被明确共享的账本流水和资产对象，不能因进入共享账本暴露个人存款或房贷。

## 5.3 顶部净资产的表达

保留一个主指标，但必须拆出来源：

```text
净资产                                      截至今天
¥216,800

资金资产 ¥190,000     计入的物品估值 ¥78,400
资金净值 ¥138,400     总负债 ¥51,600
总资产 ¥268,400
较上次完整核对 +¥3,240 · 2026/6/30
```

规则：

- `资金资产 = 账户正余额 + 投资余额 + 权益资产`，三个集合互斥；投资账户不能同时进入账户正余额和投资余额。
- `总资产 = 资金资产 + 明确计入净资产的实物可变现净估值`。
- `资金净值 = 资金资产 - 总负债`。
- `净资产 = 总资产 - 总负债 = 资金净值 + 计入的物品估值`。
- 本示例校验：`42,000 + 96,000 + 52,000 + 78,400 = 268,400`，再减负债 `51,600`，净资产为 `216,800`。
- 物品页始终可以显示物品购入成本和当前估值，不等于都进入主净资产。
- “较上次完整核对”只比较两个覆盖范围一致的 `verified_checkpoint`；不能拿“本月收入 - 支出”或任意自动快照冒充资产变化。
- 若只有部分账户核对，显示“已核对 X/Y 项”而不是全局可信变化；没有完整核对时隐藏比较值并显示“尚未完成完整核对”。
- 实物估值显示估值日期和来源；新补录的旧估值不能覆盖日期更晚的有效估值。未知估值显示“未估值”，不按 0 处理。

## 5.4 四类变化不能混名

- **收入/支出**：消费与收入分析使用，不直接等于净资产变化。
- **现金流入/流出**：账户发生额，包含转账、贷款本金、资产出售等非收支事件。
- **估值变化**：只改变存量价值，不进入普通收入、支出或预算。
- **口径/数据修正**：校准、补录、迁移、计入范围变化；必须单独标原因，不包装成经济收益或损失。

净资产变化可以由现金流、估值变化和口径修正共同造成。资产趋势若不能解释变化来源，只能标为“估算趋势”，不能标为收益曲线。

同币种转账只有在两端都属于同一净资产 scope 时才对净资产为 0；计入账户与不计入/范围外账户之间的转账会改变“已报告净资产”，但必须解释为范围内外资金移动，不是收入、消费或收益。

---

## 6. 预算 V2.1

## 6.1 核心模型不变，但再减一层认知负担

继续锁定两个独立概念：

| 概念 | 回答的问题 | 选项 |
|---|---|---|
| 浏览窗口 | 我现在想看哪段时间的执行情况？ | 本周期 / 自然月 / 自然周 / 自定义 |
| 计划周期 | 这份额度多久重置一次？ | 每月 / 每周 / 一次性专项 |

“月”永远指当月 1 日至月末，“周”永远指界面约定的自然周；只有“本周期”跟随计划锚点。例如月计划从 15 日开始时，本周期是 7/15-8/14，自然月仍是 7/1-7/31。一个周计划可以在自然月视图中按覆盖天数汇总；一个月计划可以在自然周视图中显示“本周参考额度”。

V2.1 保留可选周期锚点：

- 月计划默认每月 1 日重置。
- 高级设置可改为每月 2 至 28 日，例如 15 日到次月 14 日。
- 周计划默认周一开始，字段保留可配置周起始日。
- 主流程只显示“每月 / 每周”，起始日放在周期行的二级设置中。
- 同一 `book_id` 的任意日期最多命中一个 active `primary` 计划；创建或改期产生重叠时必须先结束旧计划或调整生效日，resolver 不做静默相加或靠创建时间猜优先级。
- `special` 不是第二份可花额度，产品文案统一为“专项追踪”。多个专项可以观察同一笔支出，但任何汇总都逐项展示，不生成专项合计后再与主计划相加。

## 6.2 执行页信息架构

```text
预算管理                                      [＋]
总账本                                      [切换 ▾]
[ 本周期 ] [ 月 ] [ 周 ] [ 自定义 ]
‹       本周期 · 7/15-8/14               ›

┌──────────────────────────────────────────┐
│ 本周期预算                                │
│ 剩余 ¥1,680                              │
│ 预算 ¥4,000        已用 ¥2,320            │
│ ━━━━━━━━━━━━━━━━╺━━━━━━━━━━               │
│ 可变支出 48% · 时间进度 39%                │
│ 待发生固定支出 ¥700                        │
│ 还剩 19 天 · 每天可自由安排参考 ¥51.58       │
└──────────────────────────────────────────┘

钱怎么花的
  固定支出（有记录时才显示）
  分类执行（默认前 5，可展开）
  未分配额度

专项追踪
  国庆旅行  ¥1,900 / ¥5,000

计划管理                              [查看全部]
```

关键变化：

- 时间进度只与可变支出进度比较；月初房租等已知固定支出按到期计划单列，不能用均匀消费假设制造“严重超前”。若没有可靠的固定支出到期数据，只显示两个中性进度，不输出“花得太快”结论。
- 沿用中性、铜金、橙色，不使用高饱和红绿灯；唯一绿色例外是低饱和预算健康态 `#7FB069`。
- 分类预算是可选增强。只填总额也能完整使用，不能强迫新用户先分十几个分类。
- 固定支出继续包含在总预算中。`预算剩余 = 总额 - 已发生净支出`；`可自由安排剩余 = 预算剩余 - 本周期未解决固定承诺`。未解决包括未来到期和已经逾期但尚未匹配/跳过的项目；逾期项目在用户确认跳过前继续保守预留，并显示“逾期待确认”。同一固定支出只能使用已匹配实际金额或未匹配计划金额，不能两者同时扣除。
- “每天可自由安排参考”仅在固定支出具有金额、到期日和已发生匹配状态时显示；否则写“剩余预算日均参考”。计划储蓄和贷款本金属于现金承诺，不进入消费预算公式，未来如展示“安全可花现金”必须另做流动性层。
- 专项追踪不进入首页“今日可用”，也不与日常预算相加成一个虚假总数。
- 执行在前，计划管理在后；编辑计划是低频动作，不占首屏。
- 四个浏览模式固定独占一行；账本切换在独立行。320dp 下优先等宽或允许整组横向滚动，不把 4 段、账本和日期硬塞一行，也不缩小标准字号。

## 6.3 本周期、自然月、自然周和自定义如何展示

### 本周期

- 月计划显示完整锚定周期，例如 `7/15-8/14`；周计划显示完整锚定周。
- 标题必须显示准确起止日期，不用“7 月”代替跨月周期。
- 左右滑切换相邻计划周期；已经结束的周期读取当时有效 revision 和 cycle override。

### 自然月

- 窗口固定为当月 1 日至月末，不因计划起始日变化。
- 月计划按自然月覆盖到的一个或两个锚定周期切片汇总；周计划按自然月覆盖到的每日额度汇总，并列出来源切片。
- 专项追踪逐项显示，不替换日常主计划，也不进入主卡额度。
- 左右滑切换自然月，年月选择器继续复用全局日历组件。

### 自然周

- 窗口固定为当前设置下的自然周。
- 周计划命中完整周期时显示真实周额度；月计划显示“本周参考额度”，不能误导为每周重置。
- 跨月或跨 revision 的周由完整周期内已经确定的每日整数分切片，界面显示来源，合计必须逐分一致。

### 自定义

- 只改变复盘窗口，不创建计划。
- 最长建议 366 天。
- 汇总窗口内日常额度、净支出和分类；专项追踪仍逐项显示，不生成可与主计划相加的总额。

所有视图都必须先在完整周期内分配整数分余数，再裁剪窗口。禁止在自然月、自然周和 Widget 中分别对同一额度重新除法。

## 6.4 新建和编辑

使用高弹层或独立页面，不再塞小窗口。顶部继续是 `SheetHeader`：左上关闭、居中标题、右上保存。

### 新建日常预算

```text
预算周期        每月 / 每周
开始时间        下周期（默认）/ 本周期
周期起点        每月 1 日 / 周一       [高级]
所属账本        总账本
周期总额        ¥____
分类额度        可选，默认折叠
参考收入        可选
固定支出        可选，可增删并真正保存
智能建议        可选，不自动提交
```

- 调整额度默认下周期生效。
- 首次在周期中途创建并选择“本周期”时，必须再明确输入语义：`本周期总额`，或 `从今天起剩余可花`。后者先计算 `新周期总额 = 截至保存时已花净额 + 用户输入剩余额度`，保存前展示换算结果。
- 编辑本周期时提供两个明确命令：`把本周期总额改为 X`、`在本周期剩余额度上追加/减少 Y`。第二个命令只用于交互，落库前转换为新的绝对周期总额。
- 周期总额、专项追踪额、分类额度、固定模板额和目标金额均为非负整数分；`adjustRemainingBy` 可以输入负 delta，但换算后的绝对周期总额不得小于 0，`setRemaining(X)` 的 X 也不得为负。周期必须 `start < end` 且自然日数大于 0。
- 禁止按修改日前后的不同日均额度拼接出第三个周期总额。日分摊只服务跨窗口展示，不能反向决定用户设置的周期总额。
- 常规修改只新增作用于未来完整周期的 revision；当前周期的特殊调整写 cycle override，不回写已经结束的周期。
- 保存前固定预览：生效周期、周期准确起止、原总额、新总额、已花、保存后剩余；若新总额低于已花可以保存，但明确显示“已超出 X”，不能静默截断消费。
- 保存时以“该周期最终有效分类额度”校验，不只看 override 自己。若降低总额后继承分类合计超过新总额，阻止保存并要求调整，或由用户明确选择“清空本周期分类额度”；禁止静默按比例缩放。分类合计小于总额时显示未分配额度。
- 月收入和固定支出只用于建议、解释和回显，不改变真实账单。

### 新建专项追踪

```text
名称            国庆旅行
起止日期        10/1 - 10/7
总额度          ¥5,000
消费范围        分类或标签，至少一种
所属账本        总账本
分类额度        可选
```

- 同一笔支出可以同时被日常计划和一个专项观察，这是两个视角。
- 两者金额绝不能相加成一个“总预算”。
- 同一笔支出可以命中多个专项追踪；页面逐项展示，不生成一个会重复计数的“专项总花费”。
- 如果未来要做“旅行期间替换日常预算”，另设高级模式，不放首版。

## 6.5 先统一计算，再迁移数据

当前代码存在一个真实不一致：月预算总额按整月逐日解析，但分类额度只取“今天生效的一条”，分类支出却统计整月。月中换计划或一次性覆盖时，总额和分类会对不上。

因此第一步不是改 UI，而是新增纯逻辑结果：

```text
BudgetWindowResult
  viewKind                  // cycle / calendarMonth / calendarWeek / custom
  viewStart / viewEnd
  scopeBookId
  plannedAmountCents
  spentAmountCents
  remainingCents
  progress
  timeProgress
  planSlices[]              // 窗口可能跨旧/新计划
  cycleSlices[]
  categoryResults[]
  fixedExpenseReferences[]
  specialResults[]
  currentCycleDailyStatus?  // 独立的当前周期指标，不混用 view 金额

CurrentCycleDailyStatus
  planId / cycleStart / cycleEnd
  cycleTotalCents
  variableSpentBeforeTodayCents / variableSpentTodayCents
  totalSpentThroughNowCents
  fixedActualSpentThroughNowCents
  fixedReserveNotYetInSpentCents
  effectiveFixedCostCents        // fixed actual + fixed reserve
  discretionaryRemainingCents
  todayRemainingAllowanceCents
  remainingDailyReferenceCents
  status / reasons[]
```

统一规则：

- 使用整数分或 Decimal，不通过 `double` 分摊。
- 月额度和周额度先在完整周期拆成每日整数分，除不尽的分按固定顺序分配；任何月、周、自定义窗口只裁剪这份稳定日历，完整周期严格回到原金额。
- 支出使用附着式退款后的原单净额，并按 `attribution_date` 进入原消费周期；账户现金流不读取这个日期。
- 转账、不计入收支和校准调整不进入预算。
- 同一账本同一天命中多个 `primary` 时 resolver 必须返回数据冲突并阻止保存，不能相加、取最新或静默选一个。
- 自然月、自然周和自定义窗口可能覆盖多个先后生效且不重叠的 primary；`planSlices[]/cycleSlices[]` 是窗口真相，单一 current plan 只允许存在于独立的 `currentCycleDailyStatus`。
- 两个日指标只基于“今天所在的 active primary 完整周期”计算，所有分子、分母和固定承诺必须来自同一 cycle。历史、未来或没有当前 active primary 时返回 `notApplicable`；自然月/周页面若展示它，只能作为独立“当前周期”信息，不得拿 view 的 planned/remaining 除以 cycle 天数。
- `timeProgress` 使用本地日历日和闭区间统一计算；历史窗口为 100%，未来窗口为 0%，当前周期按“已开始的自然日数 / 周期总自然日数”计算。所有消费者不得自行重写边界。
- 当前未结束周期默认等长比较：`N=min(当前已过日数, 上周期总日数)`，两边都取前 N 天；31 日对 30 日时比较前 30 天并明确标注。完整周期之间才比较完整总额，自然月与锚定周期不互相冒充同期。
- 金额为 0、缺少计划、无法换汇和数据冲突是四种不同状态；结果对象必须带 `status` / `reason`，不能都退化成数值 0。
- 首页、预算页、统计、Widget、月报、喵洞察和快速记账只读同一个结果。
- live 历史页以当前 knowledge cutoff 动态回看，后来退款会修正原消费期；已经生成的月报/导出冻结生成时 cutoff、scope、分类和结果，未来退款不静默改正文，重新生成时创建新版本。
- 历史月报必须按报告窗口读取当时预算，不能继续只在“当前月”使用 `repo.monthlyBudget`。

固定承诺必须按互斥分解计算，不能按状态名随意相加。对单个 occurrence，`P=planned`、`A=max(currently valid exclusive linked family net, 0)`、`H=归属日截至查询时点已经发生`；只有 link 在 query cutoff 时仍满足账本 scope、币种、周期、未撤销和唯一性约束，才算 `exclusiveLinked`：

```text
fixedActualSpentThroughNow = !skipped && exclusiveLinked && H ? A : 0

fixedReserveNotYetInSpent =
  skipped                                      -> 0
  no exclusive link                            -> P
  exclusive link && !H && no refund review     -> A
  exclusive link && !H && refund review        -> max(P, A)
  exclusive link &&  H && no refund review     -> 0
  exclusive link &&  H && refund review        -> max(P - A, 0)

effectiveFixedCost = fixedActualSpentThroughNow + fixedReserveNotYetInSpent
cycleRemaining = cycleTotal - totalSpentThroughNow
discretionaryRemaining = cycleRemaining - fixedReserveNotYetInSpent
                       = cycleTotal - variableSpentThroughNow - effectiveFixedCost
```

`variableSpentThroughNow` 必须排除已经独占匹配且归属已发生的固定交易家族。`effectiveFixedCost` 只能与 variable spent 配套；从已经减过 `totalSpentThroughNow` 的 `cycleRemaining` 再减整个 effective 值会把 fixed actual 扣两次。尚未建立独占匹配的歧义候选仍是 variable spent，同时保留整笔 `P` 并返回 partial；用户确认匹配时原子地从 variable 转 fixed actual，不能出现中间态。完整规则和反例以 `STATISTICS_CALCULATION_STANDARD.md` 为最高合同。

## 6.6 推荐数据模型

沿用上一版 plan + revision，但补周期锚点：

### `budget_plans`

| 字段 | 含义 |
|---|---|
| id / uuid | 本地和同步身份 |
| book_id | 必填，绑定真实账本视图 |
| currency_code | 首版固定 CNY；计划金额只与同币种支出比较 |
| timezone | 计划自然日使用的持久化业务时区；不随设备旅行自动改变 |
| name | 日常预算、每周生活费、旅行追踪 |
| role | `primary` / `special`；`special` 的产品文案统一为“专项追踪” |
| cadence | `monthly` / `weekly` / `one_off` |
| anchor_start_ms | 计划锚点 |
| month_start_day | 默认 1，可选 2 至 28 |
| week_start | 默认 1=周一 |
| end_ms | 专项结束或计划终止 |
| expense_scope_json | 专项分类/标签范围 |
| status | active / archived |
| created_ms / updated_ms | 审计与同步 |

约束：同一 `(book_id, currency_code)` 的 active `primary` 生效区间不得重叠。SQLite 难以只靠普通 unique index 表达区间约束，Repository 保存必须事务内查询冲突并由测试锁定。`status=archived` 只退出未来计划管理；历史 resolver 仍按 anchor/end/revision/override 读取已经发生的周期，归档不得抹掉历史额度。

### `budget_plan_revisions`

| 字段 | 含义 |
|---|---|
| id / uuid / plan_id | revision 身份 |
| effective_cycle_start_ms / effective_to_cycle_start_ms | 从哪个完整计划周期起采用这套配置 |
| amount | 周期额度，Decimal TEXT 或整数分 |
| category_budgets | 顶级分类额度 |
| monthly_income | 建议参考，回显用 |
| fixed_expense_templates | 不可变配置模板：稳定 template id、金额、周期内到期规则和可选匹配规则；不保存某月是否已匹配 |
| created_ms / updated_ms | 审计与同步 |

revision 表达“未来完整周期使用什么配置”，不切割当前周期。当前周期的特殊修改单独使用：

### `budget_cycle_overrides`

| 字段 | 含义 |
|---|---|
| id / uuid / plan_id | override 身份 |
| cycle_start_ms / cycle_end_ms | 唯一目标周期 |
| target_amount_cents | 该周期最终绝对总额 |
| category_budgets | 可选的该周期分类绝对额度 |
| input_intent | `replace_total` / `adjust_remaining` / `set_remaining`，只用于解释和审计 |
| input_delta_cents | 可选，记录用户当时追加/减少值，不作为计算真相 |
| created_ms / updated_ms | 审计与同步 |

同一计划同一周期最多一个有效 override；再次编辑更新绝对目标并保留审计事件。`category_budgets=null` 表示继承，但保存命令必须在事务内解析“revision + override”的最终配置再校验；若继承后分类合计超过新总额，强制用户写本周期分类 override 或明确清空，不能把 null 当作安全。历史报表读取该周期最终有效 override，不按创建日拼接日额度。

### `budget_fixed_commitment_occurrences`

固定支出的“模板”和“每周期执行状态”必须分表，不能让七月房租的匹配状态污染八月或回写 revision：

| 字段 | 含义 |
|---|---|
| id / uuid | occurrence 身份 |
| plan_id / revision_id / template_id | 来自哪套固定支出模板 |
| cycle_start_ms / due_date_ms | 所属周期和本次到期日 |
| planned_cents | 本周期计划金额快照；本周期调整只改 occurrence 并留审计，未来周期改 revision/template |
| resolution_status | planned / matched / skipped / requires_review；只保存业务决议 |
| review_reason | refund_after_match / ambiguous_candidate / invalid_scope / amount_conflict 等；决定预留策略 |
| matched_transaction_family_id | 匹配的原始交易家族，非散退款行 |
| resolved_ms / created_ms / updated_ms | 审计与同步 |

- `(plan_id, template_id, cycle_start_ms)` 唯一，保证每个模板每周期只有一条 occurrence。
- 首版 `(plan_id, matched_transaction_family_id)` 唯一，同一交易家族不能完整释放两个固定承诺；未来若支持合并账单再引入匹配分配额。
- `skipped` 必须清空 matched family；`matched/requires_review(refund_after_match)` 才能持有当前有效的独占 link。`ambiguous_candidate / invalid_scope / amount_conflict` 不保留 link：发现后在同一事务清空 link、写 requires_review，按未匹配计算 `actual=0 / reserve=P`；候选交易留在其真实 scope 作为 variable spent。状态、link 和审计事件必须原子更新。
- 首版只允许匹配 `root.attribution_date` 落在 occurrence cycle、账本范围和币种一致的交易家族；实际结算可晚于周期。7 月房租 8/1 扣款时，应把消费归属设为 7/31、结算日设为 8/1，或明确跳过/顺延承诺，不能用 8 月归属账单释放 7 月预留。
- 实际金额从所引用交易家族的当前净额派生，不复制一个会在退款后漂移的 `actual_cents` 真相。退款改变 family net 后，固定支出解释和可自由安排额同步重算。
- `matched_future` 和 `overdue` 是 resolver 根据 query as-of、归属日和 due date 派生的时间状态，不是需要 App 跨午夜定时改写的持久真相。提前匹配但 `root.attribution_date` 仍在未来时，family 尚未进入 `totalSpentThroughNow`，其净额继续作为 fixed reserve；到归属日才由 actual spent 接管。不能因“已匹配”提前释放房租。
- 已匹配家族发生部分退款时转 `requires_review(reason=refund_after_match)`。归属已发生时 actual 使用 family net、另预留 `max(planned-familyNet,0)`；归属未发生时尚无 actual，预留 `max(planned,familyNet)`。全额退款/撤销扣款恢复整笔计划额预留。用户可确认折扣、匹配替代账单或跳过剩余，不能让一个净额为 0 的 matched 状态释放整笔承诺。
- 逾期未匹配在查询时派生 overdue 但继续预留；只有匹配或用户明确 skipped 才释放计划额。撤销匹配/跳过走事件并恢复正确状态。歧义候选在未确认前不得写 `matched_transaction_family_id`，并以 partial + 整笔计划额保守预留。

首版预算只支持 CNY。非 CNY 支出保留在原币消费/现金流统计中，但不进入 CNY 预算；结果标明排除数量。未来多币种预算必须每币种独立计划或引入带来源的汇率，禁止直接相加。

旧 `budget_periods` 保留至少一个稳定版本。B0/B1 先仍读旧表，等所有消费者统一后再做迁移。

## 6.7 旧预算等价迁移

旧 `budget_periods` 允许 `book_id=null`、区间重叠，以及“一次性覆盖优先于循环、同级按既有优先规则取胜”。新模型禁止 primary 重叠，不能把存量直接判 conflict 或按新规则重算历史。

1. B0/B1 先用 `LegacyBudgetAdapter` 完整复现当前胜出规则，并输出逐日整数分、分类和 scope；这是迁移 golden。
2. `book_id=null` 按当前代码中已经承担该预算的真实默认/总账本 ID 映射；无法唯一证明时保留 `legacy_scope` 待确认，不擅自解释为“所有账本”。
3. 旧循环主额度物化为 plan + revision；旧一次性覆盖和周期内特殊胜出区间转为 cycle override 或只读 legacy override slice，不制造重叠 primary。
4. 迁移前后对每个受影响日期、账本、分类验证 planned cents 和胜出来源一致；完整周期/旧覆盖跨界也要逐分一致。
5. 无法等价表达的记录继续由 legacy adapter 读取并标 `needs_review`，直到用户确认；禁止为了完成迁移静默删行、合并或改历史总额。

---

## 7. 资产管理 V2.1 全流程

## 7.1 总览

```text
资产管理                                      [＋]
[ 总览 ] [ 资金 ] [ 物品 ]

净资产
¥216,800
资金资产 ¥190,000 · 计入物品 ¥78,400
资金净值 ¥138,400 · 总负债 ¥51,600
总资产 ¥268,400
较上次完整核对 +¥3,240 · 2026/6/30

结构
  流动资金       ¥42,000
  投资余额       ¥96,000
  权益资产       ¥52,000
  计入的物品     ¥78,400
  负债           ¥51,600

待处理
  2 个账户超过 30 天未校准
  信用卡 5 天后还款
  相机保修 12 天后到期

净资产趋势                [1月] [3月] [1年] [全部]
最近变化
```

首屏固定回答四个问题：

1. 净资产是多少。
2. 能较快动用的流动资产是多少。
3. 总负债是多少。
4. 和上一次覆盖范围一致的完整核对相比变化多少；若尚无完整核对，则诚实显示数据完整度。

“待处理”最多三条；无事项时整个区块消失。普通收入/支出不在资产页重复铺一遍，只显示校准、估值、还款、收回、出售、退货等资产事件。自动计算快照只用于趋势缓存，不能让“较上次完整核对”获得虚假可信标签。

## 7.2 资金

```text
搜索账户或机构
[全部] [账户] [投资] [权益] [负债]
[状态 ▾] [最近更新 ▾]

微信零钱
现金账户 · 3 天前校准                  ¥2,680.35

租房押金
2027/6 到期 · 房屋账本                  ¥8,000

房贷
每月 15 日还款 · 口径已核对             ¥420,000
```

规则：

- 账户/负债是全局对象，不显示无意义账本筛选。
- 权益可显示所属账本并筛选。
- 行点击进详情；编辑、归档、删除等低频动作收进详情菜单。
- 默认不混入归档；旧 `is_deleted=1` 继续保持隐藏，不能升级后突然复活到归档列表。
- 高价值信息是余额、类型、校准新鲜度和到期状态，不在行尾堆编辑/删除图标。

## 7.3 账户详情

```text
微信零钱                                      [⋯]
当前余额  ¥2,680.35
较上次核对 +¥128.20 · 7/1 日终

[转账] [校准余额] [编辑资料]

余额趋势                 1月 / 3月 / 1年
账户信息                 类型、机构、币种、计入净资产
最近活动                 账单、转账、校准混合时间线
```

- A1 可先做详情和近期活动。
- 余额趋势必须等 `opening_balance_effective_ms` 和校准表落地后再开，不能从未知的期初时点反推伪历史。新建账户默认以保存时精确时点为期初；历史期初若只选日期，UI 必须明确“该日日初余额”或“前一日日终余额”。
- 有历史流水的账户不物理删除，未来操作改为归档。
- “编辑资料”和“把今天余额改正确”必须是两个入口。
- 归档只把账户移出默认列表；非零余额仍进入资产/负债合计。关闭账户是另一项经济动作，必须先转空、结清或明确保留剩余价值。

## 7.4 余额校准

```text
校准余额                                      [保存]

系统计算余额        ¥2,680.35
实际余额            [¥ 2,712.80]
差额                 +¥32.45
核对时点            现在 · 2026/7/12 10:30
                    或：历史日终 · 2026/7/1
说明（可选）        微信实际余额
```

保存后写一条独立、可撤销的绝对余额锚点事件：

- `target_balance` 是该生效时点的绝对余额锚点；界面差额只用于解释“保存时系统值和实际值差多少”，不能成为后续累计真相。
- 查询某时点余额时，找到该时点之前最后一个未撤销锚点，从它的 `target_balance` 起只累计锚点之后的实际结算流水；无锚点时才从期初余额和精确期初时点计算。
- 默认“现在”使用保存时精确时间；之后发生的同日流水继续累计。只有用户主动选择“历史日终”时才使用所选日期当地日终，UI 必须明确写“该日结束时余额”，不能让一个只有日期的字段暗中采用日终语义。
- 同一时点多事件用 `effective_ms + sequence/id` 固定排序，不能依赖数据库偶然返回顺序。
- 后补锚点之前的历史账单不改变锚点之后的余额；后补锚点之后的流水正常影响余额。
- 不进入收入、支出、预算和消费统计。
- 校准也不属于真实现金流；资产变化解释中单列“余额校准/数据修正”。
- 撤销追加一条引用原锚点的撤销事件，使原锚点失效并回退上一有效锚点重算；不能写一个固定反向 delta。
- 单个账户校准只让该账户成为“已核对”，不能自动把全局净资产标为完整核对；保存后使受影响的计算快照失效并重算。
- MoneyWiz 把差额造为特殊收入/支出；肥喵不采用这种做法，避免统计污染。

## 7.5 物品

```text
我的物品                                      [＋]
在用 16 · 闲置 3 · 已结束 8
[全部] [数码] [家居] [交通] [贵重] [...]

┌──────────────────┐  ┌──────────────────┐
│      真实照片      │  │      真实照片      │
│ MacBook Pro  在用 │  │ 相机          闲置 │
│ 已持有 1,020 天    │  │ 已持有 420 天      │
│ ¥11.51 / 天       │  │ ¥18.20 / 天       │
│ 目标进度 86%       │  │ 估值 4 个月未核对   │
└──────────────────┘  └──────────────────┘
```

首页只保留最有辨识度的四项：照片、名称、状态、一个主指标。主指标默认是“日均持有花费”；估值过期、保修将到期等状态在需要时替换副信息。

性能规则：

- 两列 `GridView.builder` 懒加载。
- 两列不是硬约束：可用宽度不足或系统字体缩放达到 130% 时切为单列；200% 字体下名称、金额和状态仍不得遮挡操作。
- 图片生成缩略图，不在列表解码原图。
- 搜索和分类是首版能力，不为同一批数据做十几种展示视图。
- 数百件物品时仍要可滚动、可筛选、无一次性全量图片解码。
- 无照片使用稳定的分类占位图，不拉伸原图；只有外币、估值未知、来源账单被删和已结束物品都要有明确状态，不用空白或 `¥0` 代替。
- 卡片需有读屏名称、状态、金额和估值新鲜度摘要；图表提供文字摘要，触控热区不小于现有设计标准，系统“减少动画”时关闭非必要过渡。

## 7.6 指标口径

先锁定四个互不混用的基数：

```text
购置毛额     = source=transaction_allocations
               ? sum(allocated_gross)
               : manual_gross
购置退款     = source=transaction_allocations
               ? sum(allocated_refund)
               : manual_refund
净购置成本   = 购置毛额 - 购置退款/折让 + 未重复计入的购置费用
累计持有投入 = 净购置成本 + 维护/保险/配件等已关联净支出
可变现净估值 = max(0, 当前预计出售收入 - 预计出售费用)
实际净出售收入 = 实际出售价 - 实际出售费用
最终净成本   = 累计持有投入 - 实际净出售收入
```

购置退款只在“净购置成本”中扣一次；已经使用账单退款后净额作为购价时，不得在最终公式再次减退款。维护投入正常进入消费统计，但不自动抬高可变现估值。

金额约束：`manual_gross >= 0`、`0 <= manual_refund <= manual_gross`、购置费/维护/保险/配件金额非负；交易分配使用第 7.8 节逐条和合计约束。异常负值进入 conflict，不参与累计成本。

预计处置费高于售价时，物品资产估值下限为 0，潜在费用只作说明；只有形成真实支付义务才写账户流出或负债。实际净出售收入允许为负（例如付费报废），此时会提高最终净成本，但不能把负估值塞进“总资产”桶。

`实际持有天数 = max(1, 购买地本地自然日到结束日本地自然日的闭区间天数)`；持有中结束日取今天，已终止取经济终止事件日期。购买当天出售/退货也为 1 天，不能除以 0；跨时区不使用毫秒数除以 24 小时。购买日未知或结束早于购买日时，日均指标 unavailable 并进入待修正。

### 持有中

```text
日均持有投入 = 累计持有投入 / 实际持有天数
保值率       = 当前可变现净估值 / 净购置成本
日均价值变化 = (当前可变现净估值 - 净购置成本) / 实际持有天数
```

- 列表主指标沿用用户更易懂的“日均持有花费”，指标说明明确其计算值是日均持有投入；不叫“真实成本”“真实折旧”“使用价值”或“利用率”。
- 这个数会随时间自然下降，不代表使用更多或经济收益增加。系统不得只因时间流逝触发“更值了”庆祝；可搭配用户主动记录的最近使用、常用/闲置状态解释。
- 保值率和日均价值变化只有在存在有效可变现净估值时显示，并标估值日期/来源。
- `净购置成本 <= 0`、估值未知或币种不可折算时，保值率为“不可计算”，不能显示 `0%`。
- 某时点已有未撤销出售/退货/报废/丢失/赠送终止事件时，终止状态优先，价值按处置规则归零；生效日在终止之后的普通估值不再生效并进入冲突提示，不能把资产复活。撤销终止后才重新解析这些估值。
- 普通用户不需要每天估值。

### 已出售

```text
最终净成本     = 累计持有投入 - 实际净出售收入
最终日均净成本 = 最终净成本 / 实际持有天数
```

出售之后才有真正实现的退出成本。最终净成本小于 0 时，UI 显示“实现收益 ¥X”，不显示让普通用户难以理解的负成本。

### 使用次数

- 首版不要求逐次记录。
- 第二阶段由用户为特定物品主动开启。
- 开启后提供详情和物品卡快捷 `+1`，并支持撤销。
- 未开启时只使用“常用 / 偶尔 / 闲置”状态，不凭持有时长猜利用率。

## 7.7 物品详情

普通二级页面，不继续塞进高半屏弹层：

```text
[物品大图]
MacBook Pro                                      [⋯]
¥11.51 / 天 · 已持有 1,020 天

目标日均花费           86%
购入价                 ¥11,750
当前估值               ¥6,800 · 30 天前手动核对
保值率                 57.9%

[更新估值] [关联支出] [记录使用（已开启时）]

资料
  分类、品牌型号、购买日、购买渠道、位置、保修
关联账单
  购买、退款、维护、配件、出售
时间线
  新增、估值、折旧、状态变化、出售/撤销
```

更多菜单：编辑、折旧设置、标记闲置、出售、退货、报废、丢失、赠送、归档。每个经济终止动作必须走事件链，不能靠普通编辑绕过；归档只写可见性事件。

## 7.8 新增物品

肥喵最有价值的差异化不是复制 AI 抠图，而是复用已经记过的账单。

### 路径 A：从账单加入，主路径

入口：账单更多操作 -> `加入我的物品`。

自动带入：

- 名称/商品说明。
- 金额和退款后净额。
- 购买日期。
- 所属账本和付款账户。
- 原账单图片。

用户只需确认物品分类、状态和可选照片。目标是两步完成，不重填完整表单。

账单关联约束：

- 关联对象保存 `allocated_gross_cents` 和 `allocated_refund_cents`，物品成本来自分配额，不直接重复读取整张订单。
- 同一账单可拆给多个物品，但必须同时满足：每条 `0 <= allocated_refund <= allocated_gross`、`sum(allocated_gross) <= order_gross`、`sum(allocated_refund) <= valid_order_refunds`，因此 `sum(allocated_net) <= order_net`。只校验净额不够，因为毛额和退款额可能分别超分；同一物品与同一账单的重复关联必须拦截。
- 未分配毛额/退款允许存在，但页面显示“尚有 ¥X 未分配”；退款新增后若打破约束，进入待确认，不静默按比例改写历史物品成本。
- 关联物品保存 `cost_quality=exact / partial / pending_refund_allocation / manual_unverified`。存在未分配退款时，日均只显示“按已知成本”并带待分配提示，保值率和最终成本降级为 unavailable；确认分配后恢复精确指标。
- 一件物品可关联多笔分期/尾款账单，各笔分配额求和形成购置成本；分期还款本金本身不能再次形成物品成本。
- 首版一件物品的购置、退款、附加支出和出售按物品 `currency_code` 关联同币种事件。存在异币事件且无带来源汇率时按原币分项展示，累计投入、保值率和最终净成本标 unavailable，不直接相加。
- 一账一物时默认自动分配全额；一账多物、部分退款无法唯一分配时必须让用户确认，不能按物品数量静默平均。
- 原账单被软删除后保留关联审计和已分配金额，物品不消失；恢复或重新关联时防止重复入账。

### 路径 B：新增物品后选择已有账单

从物品页 `+` 进入，先选“从最近账单加入”，展示最近耐用品支出并支持搜索。仍调用同一关联命令。

### 路径 C：手动补录

必填只有名称；金额和日期按来源场景要求。更多资料折叠。历史已有物品不得生成账户支出。填写成本时保存 `acquisition_cost_source=manual`；以后补关联真实账单必须让用户选择“用账单分配替换手动成本”并核对差异，不能把同一笔 1,000 元手动成本和 1,000 元账单再次相加。

### 路径 D：拍照 / AI 抠图

第二阶段增强。拍照和抠图帮助识别及陈列，但跳过后也能保存，不能成为数据录入阻塞点。

## 7.9 照片与凭证的完整闭环

当前 `photo_path` / `invoice_path` 只是路径字符串，UI 仍让用户手输路径，完整备份也没有打包资产照片。V2.1 不允许把它包装成已完成的照片能力。

真实闭环必须同时包含：

1. 相机/相册选图。
2. 复制到 App 受管 `asset_media/`，不能长期依赖外部临时路径。
3. 列表缩略图和详情原图展示。
4. 删除/替换时清理孤儿文件。
5. 完整备份 package v2 打包资产媒体并恢复重映射。
6. JSON 当前账本导出继续保持当前 scope 合同，不顺手改成全库。

首版可以继续复用单封面图和单凭证字段；多附件表放第二阶段。

## 7.10 生命周期

生命周期和展示可见性是两个独立维度：

- `economic_status`：owned / sold / returned / scrapped / lost / gifted。它决定是否仍拥有价值、是否允许处置和如何进入净资产。
- `visibility_status`：active / archived。它只决定默认列表是否展示，不改变余额、估值、`include_in_net_worth` 或历史统计。

“归档”不是结束状态。持有中的物品可以归档后仍计入净资产；已出售物品也可以留在历史列表而不归档。现有把归档同时写成 `include_in_net_worth=0` 的实现必须在 A0 先修正，不能沿用到新 UI。

历史迁移边界：物品和权益不能套同一条猜测规则。

- 当前代码只允许仍持有的 active/idle 物品进入 archived，因此旧归档物品可迁为 `visibility=archived / economic_status=owned / usage_status=unknown / inclusion_quality=needs_review`。归档时已丢失原计入意图，迁移继续保留旧 `include_in_net_worth=false` 和迁移前合计，待用户确认后才改变计入范围。
- 当前权益的单一 status 允许 active、partial_recovered、recovered 或 lost 之后再被 archived，不能一律恢复为 active/owned。迁移先读取归档前最后一个未撤销经济事件和 recovery 台账：明确 lost 事件恢复 `lost`；完整收回链且 remaining=0 恢复 `recovered`；`0 < remaining < original` 且数据一致时恢复 `partial_recovered`；`remaining=original` 且无终止冲突时恢复 `active`。`remaining=0` 但无法区分收回/损失，或事件、金额互相冲突时，使用 `economic_status=unknown / inclusion_quality=needs_review`。
- 所有旧归档权益均先保持 `visibility=archived` 和迁移前 include/合计；取消归档以后只改 visibility，不能再根据 remaining 重新猜 economic status。

| 事件 | 账单/账户 | 物品 | 预算/统计 |
|---|---|---|---|
| 新购买 | 付款账户减少，保留普通支出 | 新建在用物品 | 购买支出正常进入预算 |
| 从旧账单加入 | 不新增流水 | 建物品并链接原账单 | 不重复计算 |
| 退款/退货 | 复用原订单附着退款 | 标记退货，价值归零 | 原订单净额减少 |
| 更新估值 | 不造流水 | 新估值事件 | 不进收入/支出/预算 |
| 维护/配件 | 普通支出 | 关联附加成本 | 正常进入预算 |
| 出售 | 收款账户增加，交易标资产变动 | 状态已出售、价值归零 | 不冒充普通工资/经营收入 |
| 报废/丢失/赠送 | 默认不造账户流水 | 终止状态、价值归零 | 估值损失不冒充消费支出 |
| 归档/取消归档 | 不改历史流水 | 只切换展示可见性 | 不改净资产、预算和历史报告 |

部分出售、套装拆分和多数量分批成本留后续，首版只允许整项处置。

---

## 8. 预算与资产的统一账务规则

这些规则必须先锁定，UI 才不会出现“看起来对、总数却错”的问题。

## 8.1 购买耐用品

示例：用银行卡 8,000 元买相机，初始可变现估值 7,600 元，并选择计入净资产。

```text
账户             -8,000
预算支出         +8,000
物品当前估值     +7,600
净资产变化         -400
```

购买仍是消费和预算支出；物品作为存量价值进入资产。不能因为“加入物品”就把购买从预算中排除。

若普通物品不计入净资产：账户仍减少 8,000，预算支出仍为 8,000，物品只在物品页记录，不抬高主净资产。

## 8.2 退款/退货

- 继续复用附着原单的交易家族，但补齐双日期：`attribution_date` 继承原订单消费归属日，`settled_at` 记录退款真实到账日。
- 退款继承原订单账本、币种和消费分类，但**不强制继承原付款账户**。退款事件自己的 `settlement_account_id` 表示真实到账账户，默认原账户且可改；报销到账常与付款账户不同，必须让用户确认收款账户。
- 消费、分类和预算使用 `attribution_date` 冲减原消费周期；账户余额与现金流使用 `settled_at` 在真实到账日增加。创建/更新时间只作审计，绝不替代这两个日期。
- 物品从原购买账单识别退款后，提示或自动进入“已退货”待确认状态。
- 退款减少原订单净支出；不能再造一笔独立普通收入。
- 部分退款只减少该物品的已分配净购置成本；当**该物品的有效已分配购置额被全额退回且用户确认已退货**时终止该物品。一张多物订单只退其中一件，也可以只终止对应物品；不能用“整张订单是否全退”替代物品边界。
- 原订单不计收支时，退款同样不进入收支和预算，但仍在到账日进入账户现金流。
- 全额退款后的净支出为 0；“支出笔数”“消费订单数”和“退款事件数”分别按统计标准定义，不能用同一个 count。

## 8.3 出售

示例：相机出售前估值 6,200 元，成交价 6,100 元，平台从款项中扣出售费 100 元，实际净到账 6,000 元。

```text
收款账户         +6,000
物品当前估值     -6,200
净资产变化         -200
普通收入统计          0
普通支出/预算          0
实际净出售收入   6,100 - 100 = 6,000
最终净成本       累计持有投入 - 实际净出售收入
```

出售入账可继续使用 `income + excluded=1` 的账户变动语义，但 UI 叫“资产出售”，不显示为工资或经营收入。出售费属于处置成本：若从款项中直接扣除，只保存净到账和费用拆分；若另行扣款，写独立 `asset_sale_fee` 账户移动，仍不进入普通消费支出和预算，避免与最终净成本重复。购置费若未包含在原订单金额中，则作为普通购置支出进入预算，并只在净购置成本中加一次。

## 8.4 维护、保养和配件

- 关联一笔普通支出。
- 正常进入预算和消费统计。
- 同时进入该物品的持有总成本。
- 不自动抬高当前估值；维修花 1,000 不代表二手价值增加 1,000。

## 8.5 折旧和估值

- 折旧/估值变化只改变存量价值和净资产。
- 不进入预算、收入、支出和消费分类。
- 自动线性折旧必须标“模型估算”，手工估值必须标日期。
- 市场报价未来若增加，必须保留来源、时间和失败状态。

## 8.6 负债还款

示例：本期还款 3,000 元，其中本金 2,700，利息 300。

```text
还款账户         -3,000
负债本金         -2,700
利息支出           +300
预算支出           +300
净资产变化         -300
```

本金是资产和负债同时减少，不是消费；利息和手续费才是支出。

## 8.7 存钱目标和债务目标

- 真实账户余额/负债余额决定实际进度。
- 预算只表达“本周期计划贡献多少”。
- 行情上涨、估值变化或余额校准不能伪装成预算贡献。
- 同一账户绑定多个存钱目标时，不能把完整账户余额重复算给每个目标。**首版锁定一个专用账户只能绑定一个 active 目标**，目标实际进度取该账户可用余额并随真实支出下降；多目标共享账户留到分配台账阶段。
- 账户余额只决定资金上限，目标进度取已分配金额；目标间调拨是分配变化，不是收入、支出或现金流。
- 贷款本金还款和储蓄转账不属于消费支出，但会占用流动现金。未来“安全可花现金”必须预留这些承诺，不能塞进消费预算后重复扣除。
- 心愿物品第二阶段可关联现有存钱目标；购买完成后转为物品并链接真实账单。

---

## 9. 当前代码可复用能力与缺口

## 9.1 可直接复用

### 资金

- 账户类型：现金、储蓄卡、信用卡、存款、投资、贷款、其他。
- 期初余额、机构、币种、是否计入净资产。
- 当前账户余额公式已经使用全局流水和退款净额，可复用聚合结构；历史余额因退款只有原订单日期，尚不能视为正确现金流趋势。
- 权益资产已有原始/剩余金额、到期日、部分收回、全部收回、损失、归档和撤销最近收回。
- 净资产快照表已有 `global + snapshot_date` 唯一约束和结构拆分，可作为 computed cache 的迁移基础，不能直接冒充 verified checkpoint。

### 物品

- 稳定 UUID、bookId、购买价、当前值、购买日、品牌型号、位置、保修。
- 状态：使用中、闲置、出售、报废、丢失、赠送、归档。
- 估值历史、事件、账单关联、线性折旧。
- 新购买可生成普通支出；从已有账单加入不会重复支出。
- 出售生成 excluded 账户变动，撤销出售事务化。

### 无需 DB 即可派生

- 持有天数。
- 在退款和账单分配正确的前提下，用净购置成本及累计持有投入派生日均持有投入。
- 在存在有效估值时，用可变现净估值和净购置成本派生保值率。
- 估值新鲜度必须按 valuation 的 `effective_ms` 和来源派生，不能取资产普通编辑的 `updated_ms`。

## 9.2 现有实现不能冒充完成的能力

- 实物详情写着报废/丢失/赠送应从详情操作，但当前 UI 实际没有这些入口。
- 报废、丢失、赠送没有完整撤销能力。
- 照片和发票只是字符串路径，未接真实选图和媒体备份。
- 账户没有 UUID、created/updated、期初日期和归档状态。
- 账户没有详情、校准和可信余额趋势。
- 交易只有一个业务日期；附着退款当前写回原订单日，可以算对消费净额，但不能同时算对退款到账月现金流和历史余额。
- 当前归档物品/权益会关闭 `include_in_net_worth`，把展示隐藏错误地变成净资产下降。
- 当前资产状态把 `archived` 与 sold/returned/scrapped 等经济状态混在同一枚举中，无法独立表达“仍持有但隐藏”。
- 物品账单关联没有成本分配额；一账多物、分期多账和部分退款会重复读取整单金额。
- 当前估值字段容易被普通更新覆盖；缺少“按估值生效日选最新有效记录”的唯一派生规则。
- 负债没有还款命令、详情时间线、本金/利息拆分和负债事件。
- 快照主要靠资产页手动按钮，普通记账不会自动写；表内也没有快照类型、原因、覆盖范围、币种完整度、计算版本和核对质量。
- 全局净资产使用全量物品/权益，但页面列表和详情数据按当前账本加载，导致顶部包含、列表找不到。

---

## 10. 推荐数据调整

## 10.1 先增加全局查询，不先改表

新增明确 API：

```text
globalActivePhysicalAssets
globalArchivedPhysicalAssets
globalActiveReceivables
globalArchivedReceivables
physicalAssetDetailById(id)
receivableDetailById(id)
assetEventsForObject(type, id)
assetValuationsFor(id)
```

不能只把列表从 `_physicalAssets` 换成 `_allPhysicalAssets`：当前事件、估值、关联账单和收回历史也随当前账本重载，详情查询必须一起脱离当前账本。

## 10.2 交易双日期和事件类型

现有 `transactions.date_ms` 保留为消费/收入/预算归属时间，并在领域层明确别名 `attribution_ms`；新增：

| 字段 | 含义 |
|---|---|
| settled_ms | 可空；账户真实扣款/到账时间 |
| settlement_quality | exact / user_confirmed / legacy_assumed / unknown |
| settlement_account_id | 本事件真实结算账户；普通交易可映射现有 account_id，退款/报销不强制等于原单账户 |
| settlement_account_quality | exact / user_confirmed / legacy_assumed / unknown；与日期质量分开 |
| event_type | expense / income / refund / reimbursement / transfer / asset_sale / principal_payment / interest 等 |
| refund_of | 附着的原订单；退款继承原单账本、币种和消费分类，不继承结算账户 |
| created_ms / updated_ms | 审计时间，不参与消费或现金流窗口 |

新退款写入时 `date_ms=原订单归属日`、`settled_ms=用户确认或本次记录的真实到账日`，并确认到账账户。普通 legacy 交易可暂以 `date_ms` 和现有账户迁移为 `legacy_assumed`；**现有附着退款只保存了原订单日，真实到账日已经丢失，绝不能从 `updated_ms` 或今天静默伪造**：迁移为 `settled_ms=null / settlement_quality=unknown`。旧商家退款的原账户只能标 `settlement_account_quality=legacy_assumed`；旧报销常转入工资卡，不能假定原付款账户，标 unknown 并进入待确认。

消费投影仍可按交易家族净额读取 `date_ms`。账户当前余额包含已经存在的未知日期退款金额，但历史现金流和跨该事件的历史余额结果必须标 `partial`；账户未知时分账户余额/现金流也标 partial。全局净值只有在候选账户属于相同 include scope 时可保持金额，否则同样 partial。用户确认日期和账户后才进入精确结算时间线。不能继续让一个净额投影同时服务两者。

## 10.3 `account_balance_checkpoints`

| 字段 | 含义 |
|---|---|
| id / uuid | 审计身份 |
| account_id | 账户 |
| effective_ms / sequence | 锚点生效时点与同刻稳定顺序 |
| knowledge_cutoff_ms | 创建锚点时系统已经知道哪些事件 |
| target_balance | 用户确认的实际余额 |
| calculated_before | 创建时系统计算值，仅审计展示 |
| delta_at_creation | `target - calculated_before`，仅审计展示，不参与以后计算 |
| reason | manual / liability_mode_migration 等 |
| note | 说明 |
| status | active / reversed |
| reversal_of | 撤销哪条锚点 |
| created_ms / updated_ms | 审计与同步 |

余额查询使用最后一个有效 `target_balance` 锚点；撤销只改变锚点有效链并触发重算。禁止把 `delta_at_creation` 永久累加。

legacy unknown settlement 与锚点需要额外覆盖表：

```text
account_checkpoint_covered_unknown_events
  checkpoint_id
  account_event_uuid
  PRIMARY KEY(checkpoint_id, account_event_uuid)
```

覆盖表只接收**到账账户已经确认且与 checkpoint.account_id 相同**、但到账日期 unknown 的事件。“现在核对”时，实际余额已经吸收该账户在 knowledge cutoff 前存在的 unknown-date 事件，这些 UUID 写入覆盖表，锚点后不能再次相加。历史日终锚点不能自动覆盖所有 unknown-date：用户需逐项确认事件在锚点前或后，只有确认在前者写入覆盖表；未确认则该锚点及跨越查询保持 partial。

到账账户仍 unknown 的旧报销不能被任意单账户 checkpoint 写入覆盖表，也不能默认塞回原付款账户。单账户核对可以把**该账户自锚点时点起的当前余额**确认为 target，但未知事件本身及其他候选账户仍保持 partial；必须先让用户确认目标账户，再按“现在已吸收”或历史锚点前/后规则处理 unknown date。未来如需一次核对多个候选账户，另建显式 multi-account reconciliation，不能复用单账户覆盖表猜分配。锚点创建后新录入的 unknown-date 事件同样使对应余额 partial，直到确认日期或再次做“现在核对”；新退款强制记录真实结算时间和账户，因此覆盖表主要服务 legacy 迁移。

## 10.4 `accounts` 补字段

- `uuid`
- `created_ms / updated_ms`
- `opening_balance_effective_ms / opening_balance_sequence`
- `opening_balance_quality`：exact / legacy_unknown；历史存量无法证明期初时点时不画伪趋势
- `status`：active / archived / legacy_hidden
- `archived_ms`
- `last_verified_ms`
- `verification_interval_days` 可选

旧 `is_deleted=1` 映射为 `legacy_hidden` 或继续墓碑隐藏，不能自动显示成可恢复归档。

## 10.5 资产经济状态、可见性与估值

物品/权益至少拆开：

| 字段 | 含义 |
|---|---|
| economic_status | owned / sold / returned / scrapped / lost / gifted；权益使用 active / partial_recovered / recovered / lost / unknown，unknown 仅用于迁移歧义 |
| usage_status | active / idle / unknown；与是否仍拥有分开 |
| visibility_status | active / archived，只控制默认列表 |
| include_in_net_worth | 是否进入合计，独立于归档；经济终止时按明确规则关闭 |
| inclusion_quality | confirmed / needs_review；旧归档记录因原意图丢失进入待确认 |
| acquisition_cost_source | transaction_allocations / manual，二者互斥 |
| manual_gross_cents / manual_refund_cents | 仅 manual 来源使用；后来改账单来源时被替换而非相加 |
| ended_ms | 经济终止时间，归档不写这里 |
| archived_ms | 展示归档时间 |

估值记录补齐：

```text
asset_valuations
  id / uuid / asset_type / asset_id
  effective_ms            // 估值针对的时点
  recorded_ms             // 实际录入时间
  amount / currency_code
  source                  // manual / model / market
  status / reversal_of
```

`current_value` 若继续保留，只能作为最新有效估值的派生缓存；真相是 `effective_ms` 最新的未撤销记录。补录旧估值不覆盖新估值，估值新鲜度也不读取资产普通 `updated_ms`。

估值 resolver 先判断 as-of 时点的 economic terminal event，再选择 valuation。终止优先；后续估值保留审计但标 `inactive_due_to_terminal`，撤销终止后可重新参与。

`purchase_price` 若继续保留，也只能是当前 acquisition source 的派生缓存。手动来源和交易分配来源不得同时贡献净购置成本。

## 10.6 物品媒体

第一阶段继续使用 `photo_path` / `invoice_path`，但值必须指向 App 受管媒体。完整备份升级为 package v2 并打包 `asset_media/`。

第二阶段若支持多图/多附件，再建：

```text
asset_media
  id / uuid
  asset_type / asset_id
  media_type       cover / receipt / warranty / other
  local_path
  sort_order
  created_ms / updated_ms
```

## 10.7 物品账单分配、附加支出与使用次数

`asset_transaction_links` 首阶段即补：

- `allocated_gross_cents`
- `allocated_refund_cents`
- `cost_quality`（也可由待分配状态派生并缓存）
- 唯一约束 `(asset_type, asset_id, transaction_id, link_type)`

同一原订单同时校验逐条 refund 不超过 gross、gross 分配合计不超过订单毛额、refund 分配合计不超过有效退款、net 分配合计不超过订单净额。未分配余额允许存在并明确展示；退款无法唯一映射时保存待确认分配，不静默平均。第二阶段继续扩展 `link_type`：

- maintenance
- accessory
- insurance
- other_cost

使用次数单独建事件表，支持 `count_delta` 和撤销，不把大量使用事件塞进当前资产事件枚举。

## 10.8 计算快照与可信检查点

增加统一 `_scheduleNetWorthSnapshot()` 维护 `computed_snapshot`：

- 普通交易增删改。
- 退款、转账。
- 账户和校准。
- 实物估值、折旧、出售、退货、状态。
- 权益收回/撤销。
- 负债变化。

同一天只 upsert 一条 global 计算快照；连续动作 debounce/coalesce。页面 build 不写库，快照失败不阻断主操作并在下次启动补齐。

每条计算快照至少保存 `as_of`、`cause_set`、`scope_version`、`calculation_version`、币种覆盖、缺失估值数和数据质量。它是缓存，可在补录旧流水、退款、估值或调整范围后从最早受影响时点失效并重算；历史日期快照必须计算历史日状态，禁止把今天状态写进过去日期。

`verified_checkpoint` 单独表达用户明确核对：

- 单账户校准只验证该账户，不能把全局净资产自动标为已确认。
- 全局检查点记录实际覆盖的账户、负债、权益、物品估值和币种；只有两次覆盖范围与口径一致时才显示“较上次完整核对”。
- 当前日尚未结束的自动结果标 `provisional`；口径变化、迁移和补录导致的变化单独解释，不冒充经济收益。

全局核对不能只存 coverage 标签，必须保存不可变证据：

```text
net_worth_verified_checkpoints
  id / uuid / as_of / knowledge_cutoff_ms
  scope_version / calculation_version
  currency_coverage_json
  total_assets / total_liabilities / net_worth
  status                 // active / superseded / revoked
  supersedes_id / created_ms

net_worth_verified_checkpoint_items
  checkpoint_id
  object_type / object_uuid
  confirmed_amount / currency_code
  value_effective_ms / value_source / quality
  PRIMARY KEY(checkpoint_id, object_type, object_uuid)
```

header 和 items 创建后不可原地重算。后补流水、删除对象或修改估值不改变旧 checkpoint；发现错误时新建 superseding checkpoint 或追加 revoked 状态。比较读取两份 checkpoint 当时保存的 totals/items，而不是拿当前 Repository 重新解释旧核对。

机器判定“完整核对”的必要条件：items 覆盖当时所有 `include_in_net_worth` 对象，且每个 as-of 存量余额/估值可确认；needs_review 的计入范围、无法折算的必需币种或尚未被锚点吸收的新 unknown movement 会使结果 partial。已被“现在余额”锚点吸收的 legacy unknown settlement 不再妨碍当前存量核对；待分配物品成本若当前估值已单独确认，也只作为 lineage warning，不阻止净资产完整核对。过期估值需明确接受其日期。两次比较要求 scope 政策、币种覆盖和 calculation_version 一致，且各自在 as-of 时覆盖当时全部应计对象；正常新开/关闭账户或买入/处置物品允许 UUID 集合变化，并作为变化来源解释。若是漏项或口径变化则不算直接涨跌率。`scope_version` 只因计入政策/include/权限范围改变而提升，不因正常对象新增或终止提升。

## 10.9 存钱目标资金分配

首版使用“一个专用账户只能绑定一个 active 目标”。未来开放多目标共享账户时再新增：

```text
goal_allocations
  id / uuid
  goal_id / account_id
  amount_cents / currency_code
  effective_ms
  status / reversal_of
  created_ms / updated_ms
```

保存时同币种 active allocations 合计不得超过账户可分配余额；Repository 在同一事务内校验。但账户余额随后仍可能因正常消费下降，因此还必须派生 `overallocated = max(0, allocationsTotal - max(0, accountBalance))`：不阻止记账、不静默削减或重排目标，所有相关目标暂停显示“已足额”，给出补入资金/降低分配入口。目标之间调拨只改分配，不造普通收支、账户现金流或净资产变化。

---

## 11. 负债迁移的硬风险与替代方案

## 11.1 为什么上一版不能直接做

当前公式中：

- 负债类型账户若余额 `B >= 0`，这个余额先进入总资产。
- 同时 active profile 的 `currentPrincipal=P` 又进入总负债。
- 迁移前是 `assets +B / liabilities +P / net B-P`。

若静默把同一个账户调成 `-P`，迁移后变为 `assets 0 / liabilities P / net -P`。总资产和净资产都会少 `B`。所以“直接改成负数，同时保证总资产、总负债、净资产逐分不变”在数学上不可能。

## 11.2 过渡模式

每个负债账户增加 `balance_mode`：

| 模式 | 行为 |
|---|---|
| `ledger` | 余额是唯一真相；正余额算资产、负余额算负债；profile 本金只作合同/上次核对资料 |
| `legacy_hybrid` | 完全保留当前算法；正余额算资产，active profile 本金另算负债 |

首版不再增加含糊的 `profile` 模式。未来若做独立债务对象，再单独设计。

## 11.3 可自动等价迁移

- `B < 0`：可直接标 ledger，不改数值。若 `abs(B) != P`，提示合同资料和账面欠款不同，但不改合计。
- `B = 0 且 P > 0`：用 absolute checkpoint 把余额校准到 `-P` 再标 ledger，迁移前后三项可保持一致。
- `P = 0` 或 profile 非 active：按现有计数逐账户验证后可标 ledger。
- `B > 0 且 P > 0`：一律保留 legacy_hybrid，不得静默改值。

## 11.4 歧义账户的用户辅助校准

`B > 0` 可能代表真实溢缴款/押金资产，也可能是信用额度、符号录反或过期档案。系统不能仅凭正数推荐拆出一个资产账户。先显示当前账面余额、档案本金和当前口径，并询问这个正数实际代表什么，再提供：

1. **保持现状**：继续 legacy_hybrid，无数值变化。
2. **这是我真实拥有的余额，同时另有欠款**：新建普通资产账户保留 `B`；原负债账户调到 `-P` 并切 ledger。总资产、总负债、净资产不变。
3. **这不是资产，只保留欠款**：原账户调到 `-P`，必须预览总资产和净资产会减少 `B`，用户明确确认。
4. **档案已过期**：输入实际欠款，预览后校准。
5. **这是可用额度或录反了**：不创建资产，先由用户确认正确欠款或余额语义，再预览迁移。

所有变化走 absolute checkpoint 和迁移事件，可撤销；不造普通收支。A5 必须是独立高风险批，不能与预算迁移或资产 UI 合包。

---

## 12. 分阶段实施

### C0：跨域口径门禁

- 先落实 `STATISTICS_CALCULATION_STANDARD.md` 的日期轴、事件真值表、金额精度、状态和比较窗口。
- 建立消费投影、账户移动投影、预算 resolver、资产 as-of 查询四个单一入口。
- 新指标在进入 UI 前必须登记公式、事件范围、日期轴、账本范围、币种、空值、笔数单位和反例测试。

### D0：交易结算时间地基（独立 DB 批）

- 新增 settled_ms / settlement_quality / settlement_account_id / settlement_account_quality 和明确 event_type，普通 legacy 交易等价迁移。
- 存量附着退款结算日迁移为 unknown，不从 updated_ms 猜日期；旧退款账户只作 assumed、旧报销账户 unknown；新退款/报销确认真实到账时间与账户。
- 消费投影继续按 attribution，账户移动投影切到 settlement；D0 完成后 A1 才接退货/出售新流程。

## 12.1 预算

### B0：纯逻辑地基，零 UI/DB 迁移

- `BudgetWindowResolver` 和 `BudgetWindowResult`。
- 仍读取 `budget_periods`。
- 总额、分类、周期切片和专项追踪统一解析。
- 整数分分摊和 golden 对账。
- 覆盖本周期/自然月/自然周、月中创建和改额、一次性 legacy override、跨期退款和跨月周。

### B1：执行页重构

- 本周期 / 自然月 / 自然周 / 自定义浏览。
- 主页式大卡片、普通预算进度和诚实的“按预算平均”日指标。
- B1 仍读旧 fixedExpenses，缺到期日和 occurrence，`fixedReserveNotYetInSpent` 状态为 unavailable；不得提前显示“可自由安排”或固定支出预留。该能力等 B2 数据地基落地后开启。
- 历史周期切换、分类和计划管理分层。
- 逐个迁移首页、统计、Widget、月报、洞察、快速记账消费者。

### B2：plan/revision + 周预算

- 新表迁移，旧表保留。
- 月度锚点、周起始日。
- 修改默认下周期生效。
- 固定支出 template + per-cycle occurrence 真正编辑、回显、匹配、跳过和保存；此时才开启固定承诺预留及“可自由安排”指标。

### B3：专项追踪

- 分类/标签范围。
- 产品文案统一为“专项追踪”，提供独立执行卡和归档。
- 不进入今日可用。

## 12.2 资产

### A0：信任、状态地基和信息架构（独立 DB 批）

- 先迁移 economic_status / usage_status / visibility_status / inclusion_quality；非归档记录按现状等价映射。
- 旧 archived 物品按仍持有迁移；旧 archived 权益按最后经济事件、recovery 台账和 remaining/original 矩阵恢复 active / partial_recovered / recovered / lost，证据缺失或冲突才标 unknown。两类记录都保留迁移前合计并进入必要的待确认，不静默恢复 include_in_net_worth；确认前 UI 明确显示具体未知项。
- 总览 / 资金 / 物品三视图。
- 全局 getter 和按 id 详情查询。
- 移除“本月收支净额”伪指标。
- 新归档只切可见性并完整保留使用/经济状态和 `include_in_net_worth`；展示归档移出默认列表，但不改变合计。
- 物品持有天数、日均持有花费、保值率和估值趋势。
- 补齐当前已有报废/丢失/赠送入口。

### A1：真实陈列和账单入口

- 从账单两步加入物品。
- 两列照片网格、搜索、分类、状态。
- 受管图片、缩略图、完整备份 package v2。
- 退货复用退款、出售复用资产变动。

### A2：账户详情和全局趋势

- 账户详情、近期活动。
- computed snapshot、数据质量和变化原因。
- 只开放明确标为估算的全局净资产趋势；不显示“完整核对变化”。
- 暂不画账户余额趋势。

### A3：余额校准 DB 批

- absolute checkpoints、账户 UUID/时间戳/期初日期/状态。
- 校准、撤销、账户归档。
- 账户余额趋势从可信起点开始。
- 完整核对流程和 verified checkpoint；覆盖不完整时只标部分核对。

### A4：物品和权益的增强闭环

- 保修提醒、维护支出关联。
- 可选使用次数和快捷 `+1`。
- 心愿物品关联存钱目标。
- 完整撤销矩阵和待处理事项。

### A5：负债单真相，高风险独立批

- `ledger / legacy_hybrid`。
- 等价自动迁移和歧义账户辅助校准。
- 还款本金/利息拆分、事件和撤销。
- 结清与归档。

### A6：明确暂缓

- 证券持仓、份额、成本、市价和收益率。
- 自动汇率和多币种折算。
- 自动房产/车辆/贵金属估值。
- 套装部分出售、库存和复杂折旧。

## 12.3 推荐总排期

```text
1. C0 统计与账务口径门禁
2. B0 预算统一解析
3. A0 资产口径、归档修正与三视图
4. B1 预算浏览重构
5. D0 双日期与账户移动迁移
6. A1 物品陈列与账单分配
7. A2/A3 详情、计算快照、绝对校准和可信核对
8. B2 周预算、revision、cycle override 与固定 occurrence
9. B3 专项追踪
10. A4 物品增强
11. A5 负债统一
```

先把数字“看得懂、找得到、算得对”，再增加需要维护的新能力。

---

## 13. 验收标准

## 13.1 预算

1. 月计划 3,000 元，任意历史周期显示当时 revision，不随今天修改而变化。
2. 周计划 700 元，完整周严格为 700；跨月两部分之和仍为 700。
3. 自然月汇总周计划时，主卡与周期切片逐分一致；完整周在两个自然月中的切片相加仍为原周额度。
4. 自然周展示月计划时明确写“参考额度”，不冒充周计划。
5. 每月 15 日起始的计划在“本周期”显示 7/15-8/14，在“自然月”仍显示 7/1-7/31，并由前后周期切片组成。
6. 月中把 3,100 改为 6,200 时，本周期总额严格为 6,200，不得按旧新日额拼成 4,700。
7. 首次月中创建分别验证“本周期总额 X”和“从今天起剩余可花 X”，保存前换算和预览可解释。
8. 同一账本主计划区间重叠时禁止保存；专项追踪可重叠但不形成可相加总额。
9. 跨月退款按原消费归属日冲减预算，到账月普通收入仍为 0。
10. 迁移前已有附着退款若无真实到账日，消费/预算仍正确，历史现金流标 partial 并进入待确认，不能用 `updated_ms` 伪造。
11. 固定支出保存后重启可回显；预留后匹配实际支出时只计算一次。无法可靠匹配时不显示“安全可花”。
12. 金额无法整除周期天数时，先在完整周期固定余数，再裁剪所有窗口。
13. 当前未结束周期与上周期同期比较；历史完整周期与完整上周期比较。
14. 首页、预算、统计、Widget、历史月报和 AI 对同一窗口、账本及口径一致到 1 分。
15. 分类合计超过总额禁存，未分配额度允许存在；新总额低于已花时显示超支而不改历史账单。
16. 320dp、深色、六主题、200% 字体和读屏下日期、金额、状态及保存操作可用。
17. 计划归档后不再生成未来周期，但历史周期、revision、override 和执行率保持不变。
18. 首版 CNY 预算不合并非 CNY 支出；页面显示被排除的原币笔数，不把它们当 0 或偷偷换汇。
19. 降低本周期总额导致继承分类额度超额时阻止保存，直到用户调整或明确清空分类额度，不自动缩放。
20. 同一个房租模板在七月和八月生成独立 occurrence；七月匹配/跳过不改变八月和历史 revision。
21. 同一交易家族不能同时完整匹配两个固定承诺；退款改变家族净额后实际固定支出同步更新。
22. 逾期未匹配固定承诺在两个日指标中都继续预留并显示待确认，用户跳过后才一致释放。
23. 旧 `book_id=null`、重叠循环和一次性覆盖经 adapter/迁移后，每个日期的胜出额度、分类和账本范围逐分一致；无法等价者留 legacy 待确认。
24. 7 月 occurrence 只能匹配消费归属在 7 月周期内的家族；8/1 实际扣款可用 `attribution=7/31, settled=8/1`，不能用 8 月消费释放 7 月预留。
25. 已匹配固定支出部分退款后差额继续预留并待确认；全额退款不会保持 matched 并错误释放整笔额度。
26. 已匹配家族后来发生 scope/币种/撤销/金额冲突时，旧 link 原子清除，fixed actual 归零、整笔 P 恢复预留，交易只留在真实 scope 的 variable spent，结果标 partial。

## 13.2 资产总览与范围

1. 切换任意抽屉账本，顶部 global 净资产、资金净值和总负债不变。
2. 资金/物品筛选只改变列表，不偷改顶部。
3. 顶部合计中的每一项都能进入对应清单和详情找到。
4. 私有且不计总账本的物品仍可在“全部”中进入详情并看到事件/估值。
5. `included/total` 不出现分子大于分母。
6. 非 CNY 不进入人民币合计，原币金额仍可见。
7. 只有外币且无汇率时显示原币与“不含外币”状态，不显示人民币 `¥0` 或伪完整净资产。
8. `资金资产 190,000 + 物品 78,400 = 总资产 268,400`；减负债 51,600 后净资产必须为 216,800。
9. 归档非零账户、持有中物品或未结权益只隐藏列表，顶部净资产逐分不变。
10. 旧 archived 资产迁移后合计逐分不变，使用状态和原计入意图标 needs_review；用户确认前不静默重新计入。旧权益须按事件恢复 recovered/lost、按金额恢复 partial_recovered；remaining=0 且无证据时为 unknown，不得一律恢复 active/recovered。
11. computed snapshot、部分账户核对和完整 verified checkpoint 使用不同文案；普通记账不能自动获得“完整核对”标签。
12. 补录旧流水或改变计入范围触发受影响历史快照重算，并把变化标为数据/口径修正，不在今天制造虚假收益。
13. 共享账本成员无法看到未明确共享的个人账户、房贷、权益和物品。
14. 首版同一专用账户绑定第二个 active 存钱目标时阻止保存；未来分配台账中，分配后账户消费导致余额不足时显示 overallocated，不拦截消费也不静默改目标。
15. 完整 verified checkpoint 创建后，补录流水、修改估值或删除对象不改变其 header/items；纠错生成 superseding/revoked 记录，比较仍可审计。

## 13.3 物品

1. 从已有账单加入不生成第二笔支出。
2. 新购买同时记账时，账户、预算、物品估值和净资产变化符合第 8 节公式。
3. 退款后原订单净额和已分配物品净购置成本一致，退款只扣一次；某物品的有效分配额全退且用户确认退货后只终止该物品，不要求多物订单整单全退。
4. 出售后账户增加、物品价值归零，普通收入统计不增加。
5. 持有天数、日均持有投入、保值率和最终净成本使用第 7.6 节统一基数。
6. 未核对估值明确显示估值日期、来源和过期状态；未知值不写成 0 或实时净值。
7. 照片选取后完整备份、恢复到新路径，列表和详情仍可见。
8. 数百件物品时使用懒加载和缩略图，无全量原图解码。
9. 报废、丢失、赠送、出售、退货均有经济事件和允许范围内的撤销；归档只切换可见性。
10. 普通消费品默认是否计入净资产符合类型推荐，但存量开关绝不静默改变。
11. 一张订单创建两个物品时，分配净额合计不得超过订单净额；重复关联被拦截。
12. 一件物品关联多笔分期时只累计被分配的购置款，不把分期还款本金重复当成本。
13. 先录新估值再补录旧估值，当前估值仍取生效日更晚的有效记录。
14. 窄屏或字体达到 130% 时物品网格改单列；200% 字体、无图和估值未知状态仍可完成处置。
15. 购买当天出售或退货时持有天数为 1，日均指标不除零；结束早于购买日进入待修正。
16. 手动补录成本 1,000 后关联同一笔 1,000 账单，净购置成本仍为 1,000，必须替换来源而非相加。
17. 新退款尚未分配到多物订单时，相关物品 costQuality 降级，保值率/最终成本不显示伪精确值。
18. 一件物品存在异币购置/维护/出售且无汇率时分币种展示，累计投入和最终成本 unavailable。
19. 先录 7/10 估值再补录 7/1 出售/退货时，终止事件优先，后续旧估值不能把资产复活；撤销终止后才重新解析估值。

## 13.4 校准、快照与负债

1. 校准后余额精确等于目标值，收入、支出、预算和分类统计不变。
2. 历史日期校准只影响该日及之后。
3. 校准后补录锚点之前的旧账，锚点之后余额保持不变；补录之后的流水正常累计。
4. 连续两个校准后撤销第二个，余额回退到第一个有效锚点重算；不靠固定反向 delta。
5. 同日流水、校准与撤销按明确 sequence 得到稳定结果。
6. 旧 deleted 账户不会重新出现在归档列表。
7. 负债迁移覆盖 `B<0 / B=0 / B>0` 与 `P=0 / P>0 / inactive` 矩阵。
8. 自动等价路径的总资产、总负债、净资产逐分不变。
9. `B>0` 先确认代表真实资产、额度还是录入错误；任何拆分或只留欠款路径先展示差异并由用户确认。
10. 等价迁移同日 computed snapshot 不产生虚假净值波动，也不自动生成 verified checkpoint。
11. 本金还款不进支出，利息/手续费进入支出和预算；两者都按结算日进入账户现金流。
12. 单账户校准只标该账户已核对；覆盖范围不足时全局只能显示部分核对。
13. 已知到账账户但 unknown-date 的 legacy 退款在“现在核对”后可由同账户 checkpoint 覆盖，当前余额不重复相加；到账账户 unknown 的旧报销不能被原付款账户或任意单账户 checkpoint 覆盖，先确认目标账户。
14. 上午 10:00 “现在核对”余额 100，下午 15:00 支出 20 后余额为 80；只有显式“历史日终”才使用所选日期日终锚点。

---

## 14. 推荐直接锁定的产品决策

| 编号 | 决策 | 推荐 |
|---|---|---|
| D1 | 资产总览是否跟随当前账本 | 固定全局；账本只作物品/权益筛选 |
| D2 | 资产页一级结构 | 总览 / 资金 / 物品 |
| D3 | 普通物品是否默认计入净资产 | 不强迫；存量保留，重大资产按类型推荐开启 |
| D4 | 物品主指标 | UI 叫日均持有花费，公式用累计持有投入/天；不等同利用率、收益或真实折旧，不因时间流逝单独庆祝 |
| D5 | 物品新增主路径 | 从已有账单两步加入；一账多物和多账一物必须保存分配额 |
| D6 | 次均成本 | 第二阶段、用户主动开启 |
| D7 | 账户差额 | 绝对 target_balance 锚点；差额只作创建时审计，不改期初、不造普通收支或真实现金流 |
| D8 | 负债歧义迁移 | 保留 legacy_hybrid，用户辅助校准，禁止静默改值 |
| D9 | 预算浏览 | 本周期 / 自然月 / 自然周 / 自定义；“月”永远是自然月 |
| D10 | 月计划起始日 | 默认 1 日，高级设置支持 2 至 28 日 |
| D11 | 调整预算默认生效 | 默认下一完整周期；本周期修改保存绝对周期总额，不拼接新旧日额 |
| D12 | 专项能力 | 文案叫“专项追踪”，不额外增加可花额度，不与主计划相加 |
| D13 | 证券行情/自动汇率 | 暂缓，先保留投资账户余额和原币数据 |
| D14 | AI 抠图 | 展示增强，不作为首版必经流程 |
| D15 | 退款日期 | 消费/预算归属原订单日，账户现金流归属真实到账日，必须双日期 |
| D16 | 归档 | 只改变可见性；经济终止和 include_in_net_worth 由独立状态/动作决定 |
| D17 | 快照可信度 | computed snapshot 可重算；verified checkpoint 必须明确核对并保存覆盖范围 |
| D18 | 未知与零 | 未知、部分可计算、过期、不可折算和真实 0 分开表达 |
| D19 | 存钱目标 | 首版一个专用账户只绑定一个 active 目标；未来分配台账必须处理余额下降后的 overallocated，禁止重复占用 |
| D20 | 统计合同 | 收支、预算、现金流、余额、净资产、笔数和比较统一服从 `STATISTICS_CALCULATION_STANDARD.md` |

这些决策在 V2.1 中视为锁定。实施阶段无需再次反复询问；只有出现数据迁移无法等价、共享权限冲突或用户主动改变产品方向时才重新打开，A5 具体迁移 UI 和证券级能力仍需单独评审。

---

## 15. 来源台账

统一访问日期：2026-07-12。

## 15.1 物品资产型

| ID | 来源 | 支持的主要事实 |
|---|---|---|
| YS01 | [有数 App Store](https://apps.apple.com/cn/app/id6739918572) | 官方定位、日均成本、数字陈列、卖出盈亏、评分和版本 |
| YS02 | [有数评论页](https://apps.apple.com/cn/app/6739918572?see-all=reviews) | 美观/日均成本反馈、购买平台、部分出售、评价弹窗等可见评论 |
| CW01 | [持物 App Store](https://apps.apple.com/cn/app/id6747913105) | 维护费用、使用记录、结构图、本地+iCloud、贵金属估值和版本说明 |
| CW02 | [持物评论页](https://apps.apple.com/cn/app/6747913105?see-all=reviews) | 使用次数维护、资产库、同步和限制反馈 |
| ZW01 | [知我记物 App Store](https://apps.apple.com/cn/app/id6740251522) | 多渠道录入、附件、使用次数、保修、回顾、心愿单 |
| ZW02 | [知我记物评论页](https://apps.apple.com/cn/app/6740251522?see-all=reviews) | 大量物品性能、筛选、信息密度和快捷使用反馈 |
| YW01 | [与物 App Store](https://apps.apple.com/cn/app/id6757421355) | 日均/次均成本、附加费用、退出、心愿和隐私 |
| YW02 | [与物评论页](https://apps.apple.com/cn/app/6757421355?see-all=reviews) | 极简体验和附加费用口径反馈 |

## 15.2 钱往

- QW01 [App Store 官方描述与版本说明](https://apps.apple.com/cn/app/id6446384233)
- QW02 [官方 FAQ](https://slog.tech/about/qa.html)
- QW03 [1.13.x：净资产、负债分析、余额调整](https://slog.tech/changelog/apple/1.13.x.html)
- QW04 [1.16.x：资产导入、预算报销返还、净资产修复](https://slog.tech/changelog/apple/1.16.x.html)
- QW05 [1.17.x：日历现金流与资产收益](https://slog.tech/changelog/apple/1.17.x.html)

## 15.3 MoneyWiz

- MW01 [在线银行持续连接或一次导入](https://help.wiz.money/en/articles/4440632-creating-an-online-banking-account)
- MW02 [手动账户类型](https://help.wiz.money/en/articles/4440641-creating-an-account-managed-manually)
- MW03 [简单/高级 reconciliation](https://help.wiz.money/en/articles/4440674-how-to-reconcile-transactions)
- MW04 [余额调整行为](https://help.wiz.money/en/articles/4440697-how-to-adjust-account-balance)
- MW05 [贷款本金、APR、期限和本金利息拆分](https://help.wiz.money/en/articles/4440722-how-to-track-loans)
- MW06 [投资持仓和报价](https://help.wiz.money/en/articles/4440724-how-to-track-traditional-investments)
- MW07 [Net Worth / Liabilities / Cash / Today](https://help.wiz.money/en/articles/5545985-what-do-statistics-above-the-list-of-accounts-mean)
- MW08 [简单预算](https://help.wiz.money/en/articles/4838500-setup-simple-budgeting-system)
- MW09 [Envelope 预算](https://help.wiz.money/en/articles/4838574-setup-envelope-budgeting-system)

## 15.4 Monarch Money

- MO01 [连接或手动账户](https://help.monarch.com/hc/en-us/articles/360048393392-Connect-or-Manually-Add-Accounts)
- MO02 [刷新与同步状态](https://help.monarch.com/hc/en-us/articles/360054839131-Refreshing-Your-Accounts)
- MO03 [低频变化的手动账户](https://help.monarch.com/hc/en-us/articles/360058187072-Manual-Accounts)
- MO04 [编辑近 90 天余额历史](https://help.monarch.com/hc/en-us/articles/32368722344212-Manually-Edit-an-Account-Balance)
- MO05 [月度 cash-flow 预算与账户分工](https://help.monarch.com/hc/en-us/articles/360048883631-Creating-Your-Budget-in-Monarch)
- MO06 [Fixed / Non-monthly / Flex](https://help.monarch.com/hc/en-us/articles/32125337244052-Using-Flex-Budgeting)
- MO07 [投资、净资产和预算](https://help.monarch.com/hc/en-us/articles/41855507661076-Investments-in-Monarch)
- MO08 [Goals 3.0](https://help.monarch.com/hc/en-us/articles/44373110771860-Introducing-Goals-3-0)
- MO09 [Save Up Goals](https://help.monarch.com/hc/en-us/articles/44373182867476-Using-Save-Up-Goals)
- MO10 [Pay Down Goals](https://help.monarch.com/hc/en-us/articles/44373293932052-Using-Pay-Down-Goals)
- MO11 [Needs Review](https://help.monarch.com/hc/en-us/articles/5528707082516-Reviewing-Transactions)

## 15.5 YNAB

- YN01 [Daily / weekly / monthly routine](https://www.ynab.com/blog/five-minute-budget-routine)
- YN02 [Reconciliation](https://www.ynab.com/blog/budget-reconciliation-in-ynab)
- YN03 [预算与账户的关系](https://www.ynab.com/blog/the-relationship-between-your-budget-your-accounts-its-complicated)
- YN04 [Tracking accounts 的用途和维护负担](https://www.ynab.com/blog/the-how-and-why-of-tracking-accounts)
- YN05 [无行动价值就不必追踪](https://www.ynab.com/blog/what-is-the-point-of-a-tracking-account)
- YN06 [信用卡预算预留](https://www.ynab.com/blog/how-to-do-credit-cards-in-ynab)
- YN07 [Loan Planner](https://www.ynab.com/blog/ynab-loan-planner)

## 15.6 Actual Budget 与 Cashew

- AC01 [Actual Budget 官方首页](https://actualbudget.org/)
- AC02 [On-budget 与 off-budget 账户](https://actualbudget.org/docs/accounts/)
- AC03 [Reconciliation 与估值校准](https://actualbudget.org/docs/accounts/reconciliation)
- AC04 [银行同步边界](https://actualbudget.org/docs/advanced/bank-sync)
- AC05 [本地和自托管同步](https://actualbudget.org/docs/getting-started/sync)
- CA01 [Cashew App Store](https://apps.apple.com/us/app/id6463662930)

## 15.7 代码证据

- `lib/data/app_repository.dart`：账户、物品、权益、负债、快照、事件、估值和 CRUD。
- `lib/views/settings/accounts_view.dart`：当前单页信息架构、手工路径式图片、缺失的生命周期入口。
- `lib/core/budget/budget_period.dart`：现有按日月总额解析和优先级。
- `lib/views/settings/budget_setting_view.dart`：当前分类窗口、fixedExpenses 未保存和编辑历史风险。
- `lib/core/budget/budget_suggestion.dart`：固定支出包含在总预算中的既定口径。
- `lib/views/settings/import_export_view.dart`：资产 JSON 当前账本导出合同。
- `test/app_repository_test.dart`：资产守恒、账单关联和跨账本现状。

## 15.8 冲突与失败记录

- Monarch 一篇投资 FAQ 说买卖按 transfer 不影响预算；较新的 beta 文档说开启投资交易后可以进入预算和 cash flow。本文不依赖该冲突，肥喵自行锁定“买卖本金不进预算，费用进支出”。冲突页：[Turning on Investment Transactions](https://help.monarch.com/hc/en-us/articles/22373197208212-Turning-on-Investment-Transactions)。
- `support.ynab.com` 在本环境连接中断，未使用搜索摘要，改用同公司 `ynab.com` 官方文章。
- 47 个外部链接完成自动状态审计：35 个返回 HTTP 200；12 个 Monarch Zendesk 官方链接因自动访问防护统一返回 HTTP 403，已保留为官方来源并记录限制，没有把 403 当作内容失效。
- 有数官方资料没有解释“总资产净值”的精确估值公式，本文没有假定它等同于严谨净资产。
- 本文关于打开频率的内容，除 YNAB/Actual 明确操作建议外均标为产品推断，不冒充行为分析数据。
