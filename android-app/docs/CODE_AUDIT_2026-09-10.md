# Android 代码复核与修复（2026-09-10）

## 基线与边界

- 基线：本地主工作树 1813fe2；保留在途 VPS 部署文件，不修改 iOS 或指定 parity integration_test。
- 候选版本：1.291.0+306 / b0910-306。305 已被 VPS 专用回退包占用。
- 本轮构建不代表授权发布；生产分发遵循 VPS_UPDATE_DEPLOYMENT.md，不运行旧 CF 发布流程。

## 重新核实的结论

| 项目 | 结论与本轮范围 |
|---|---|
| 缺失 APK 哈希放行 | 确认缺陷；校验函数缺失/非法哈希返回 false，下载无元数据或响应头哈希时拒绝完成；新客户端直连 VPS 更新和回退目录 |
| 延迟初始化重试 | 确认失败 Future 被缓存；失败后释放 Future，重新创建完成屏障，成功清除错误；维护标记仅在成功后清除 |
| AI 取消 | 确认仅取消上层等待；V2 托管请求创建独立传输资源，在取消或结束时关闭，不关闭其他任务共享连接；不能承诺服务商收到请求后一定停止计费 |
| 备份恢复内存 | 确认整包及全部解压数据驻留；改为后台 isolate 读取磁盘 ZIP，逐文件 SHA256/CRC 检查、写入临时目录；保留数据库 quick_check 和原子恢复/回滚 |
| 备份限制 | 最多 10000 条目，单条目 128 MiB，声明总解压量 2 GiB，manifest 4 MiB；拒绝符号链接和不安全路径。不是不受限的任意大备份支持，也不是逐字节有界的解压器 |
| 全局缓存版本、长聊天全量构建 | 确认实现存在；属于需基准测试的性能问题，本批不重做列表与 UI |
| 附件内联内存 | 单文件限制不能替代总消息预算；仍待独立修复，不列为完成 |
| 启动最终数据与重复全量读取 | 需要维护前后数据一致性和性能专项；不凭静态代码宣布性能已提升 |
| 巨型仓库/聊天文件 | 架构债务，不是仅凭行数即可认定的功能错误；不进行一次性重写 |

## 当前验证

- 独立 SDK：官方 Flutter 3.44.2，下载包 SHA256 d79ae99807ba744b843e54f048308c629061456fdc7b0753251fb96eb5346a0e。
- 定向测试 30/30 通过；包含故障数据库修复后的初始化重试、取消资源隔离、磁盘备份解码、更新哈希与 VPS 地址。
- 修改模块静态分析通过；全项目 analyze exit 0，90 条 warning/info，无 error。
- 全量 Flutter 测试 1216/1216 通过（约 6 分钟）；发布身份规则测试 9/9 通过。
- Flutter 正式 Release 构建成功；APK 包名 com.qingji.qingji.codex、版本 1.291.0+306；zipalign 16 KiB exit 0、apksigner exit 0、V2=true，单一证书 DN 与固定证书 SHA256 4E99C399D4D246BD9C6B08B1D641248BD0846E7AE650C3A766E30FA67483D507 匹配。
- APK：`ci-artifacts/releases/feimiao-codex-v1.291.0-306.apk`，117920317 字节；SHA256 `9F53F8216BA8671C2DED45A73CB6A09B51EF253B08DEC951AE9B8A2CD8B658A6`。
- 构建修复仅环境级：官方独立 SDK；Gradle 临时使用系统代理和 HTTPS 镜像缓存依赖；正式 Flutter build apk --release 成功。不要在测试注册文件生成之后用 --no-pub 跳过 Release 插件注册重生成。
- Bash 门禁入口因本机 dirname 缺失未运行成功，改用同版本 Android Build Tools 原生执行 aapt、zipalign、apksigner 并核对全部身份字段；不称原 Bash 脚本通过。
- 当前 ADB 无在线设备；启动现有模拟器请求被审批系统 429 拒绝，未绕过：安装、冷启动、真实网络取消和手机备份内存峰值尚未验收。未提交或推送 Git。
- 用户随后授权发布：VPS 已上线 `v306-9f53f8216ba8`；现有发布脚本 exit 0，新旧版本入口一致、回退目录可读，服务器保留 304/305/306 共三包。公网完整下载因速度过慢已主动停止，未将其报告为完整 SHA256 复验通过；本地与服务器端哈希已由发布门禁校验。

日志：项目 `.tmp/flutter-audit-tests.log`、`.tmp/flutter-audit-analyze.log`、`.tmp/flutter-audit-build.log`。
