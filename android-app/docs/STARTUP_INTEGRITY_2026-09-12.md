# 启动完整性修复（2026-09-12，1.293.0+308 已发布）

## 修复范围

- 首页快照先恢复分类图标样式、完成退款归并、到期预算周期与周期账生成，然后查询当月流水并发出 home-ready。没有待归并负数支出时先短查询退出，不把全量流水读进内存。
- 全历史与资产仍延迟加载；公共 AppPageRoute 在 hydration 期间先显示加载状态，不构建依赖完整数据的页面；首页切历史月也受保护，月份选择器等待完整历史后读取记录。
- 初始化失败后的重试按 core/deferred 区分，重新接回后续加载、代理/OAuth 与后台服务流程；后台服务按仓库实例只挂接一次，并等待真实 fully-ready。连续点击重试不会重复启动流程。
- 已消费的外部分享请求在初始化失败时保留于当前进程队列，不清空、不循环忙等；恢复后明确唤醒，正常消费后移出队列。尚未实现跨进程终止的分享队列持久化。
- AI 等待历史加载后检查 isFullyReady；加载失败不能继续使用部分历史发起查询。
- 核心初始化失败会等待旧 AI 加载结束后释放其 Future，避免重试复用失败的 AI 加载状态。

## 验证与边界

- 新增/更新定向回归：12 项通过；涵盖分享保留/恢复、历史正文延迟构建、核心与后续失败重试接回服务、首屏退款与图标设置、到期周期账幂等，以及已有抽屉手势。
- 全量回归 1221/1221 通过（约 4 分 47 秒）；加载态最后补充的 Material 包装另跑 11 项定向测试通过。全项目 analyze exit 0，无 error，90 条 warning/info；日志 `.tmp/startup-integrity-final-tests.log`、`.tmp/startup-integrity-analyze.log`。
- 未改变既有数据库版本、iOS 和指定 parity integration_test 文件。用户随后授权构建发布，版本递增 1.293.0+308 / b0912-308。
- 发布前定向再测 12/12、发布身份规则 9/9；正式 Flutter Release 构建、固定证书/V2/16 KiB 校验通过。日志 `.tmp/startup-308-gate.log`、`.tmp/startup-308-build.log`。
- APK `ci-artifacts/releases/feimiao-codex-v1.293.0-308.apk`，SHA256 `73DF20B0BA3C1A3AF1CFE5F0F9EF7CC0F0A38C5DF155239493A54CF3DCDC213E`，117936705 字节。
- VPS 发布 `v308-73df20b0ba3c` 成功；新旧更新入口与哈希元数据一致，Range 206 前1024字节匹配。完整公网重新下载哈希未验证；服务器端完整哈希已校验。线上保留 306/307/308，305 已清理且本地原包保留，回退目录无条目。
- 需要设备复验真实冷启动耗时、分享恢复与原生后台服务；本轮不声称性能更快。加载态 UI 尚无实机前后对比图。
- 启动必要账务维护前移是正确性取舍；大量游离退款或长期未打开的周期账仍可能增加 home-ready 时间，后续须基于数据规模测量再优化，不能将数据未完成显示算成启动完成。
