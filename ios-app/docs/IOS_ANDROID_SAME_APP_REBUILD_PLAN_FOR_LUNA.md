# 肥喵记账 iOS 同款重建方案（Luna 执行版）

更新时间：2026-09-02

## 0. 结论先行

用户目标只有一个：**iOS 版就是现有 Android「肥喵记账」在 Apple 平台上的原生实现，不是重新设计的新记账软件。**

当前 iOS 不是“功能全无”，真正的问题是：仓库同时存在两套 iOS 方向，截图证据又混用了不同提交、不同日期和不同页面。结果是部分业务能力已经很多，但入口、页面骨架、品牌组件和首屏信息经常按“通用 SwiftUI App”重新组织，用户自然会觉得它是另一款软件。

本方案的核心决策如下：

1. **Android 当前生产 App 是唯一产品母版。**入口、信息层级、页面顺序、字段、文案语义、品牌素材和业务结果均以 Android 为准。
2. **保留功能更完整的 `ios-app/QingJi` 作为生产功能主干。**它已有 SwiftData、核心账务、预算、资产、AI、OAuth、备份、Widget、Share Extension、App Intents 等大量能力。
3. **不得把 `ios-app/FeiMiao` 整套替换为生产 App。**该分支基于较旧的 Android `1.197.0+199`，使用另一套 GRDB 数据层、Bundle ID、版本线和工程目标，只完成批次 1–5；整套切换会造成已完成业务能力大幅倒退，并形成第二款 App。
4. **只审计性移植 `FeiMiao` 中有价值的同款 UI。**可以移植品牌素材、主题 token、推开式抽屉、首页汇总卡、筛选、按日卡、底部记账入口、按压反馈等；不得移植它的 `AppStore`、`FeiMiaoKit`、GRDB 数据库、`project.yml`、Bundle ID 和版本线。
5. **先关闭证据链，再按“壳层 → 核心记账 → 业务管理 → 资产 → AI/系统 → 全量交付”推进。**每个小批次最多处理一个完整用户链路或 2–4 个紧密页面，完成即复盘；本批范围内的缺陷和回归必须归零，明确属于后续阶段的计划项必须写明唯一主责阶段、验收条件和依赖，不能伪装成已完成。

这不是“像素复制 Android”。iOS 可以保留状态栏、安全区、系统键盘、日期选择器、照片/文件选择器、触觉、VoiceOver、侧滑返回和系统权限弹窗；但这些原生能力不能改变页面身份、入口层级、字段顺序或业务结果。

### 0.1 复盘后的可执行性结论

原始版本存在四个会阻碍交付的问题：P0 与后续业务阶段循环依赖、多个场景没有唯一主责阶段、缺少已有 iOS 数据升级合同、发布门禁偏向截图和构建而缺少完整用户旅程。本文已按以下原则修订：

- P0 只关闭“母版、工程起点、页面身份、固定时钟、fixture、metadata 和验收合同”，不要求尚未实现的 P1–P5 页面在 P0 先变成同款；
- 每个场景只有一个主责阶段，P0 只负责取证，P6 统一复验；
- 每批只要求本批范围内缺陷为 0，后续计划项必须有阶段归属，禁止用“以后再说”掩盖回归；
- 最终完成必须同时通过界面、业务结果、数据升级、真实系统能力、真机用户旅程和可回退发布六类门禁。

**按修订后的计划完整执行并通过 P6，可以做成真正的 iOS 版肥喵；只做到截图相似、模拟器可运行或 CI 出 IPA，仍不算完成。**

---

## 1. 当前仓库与证据状态

### 1.1 三条并行线必须先收口

| 位置/分支 | 当前性质 | 可复用内容 | 禁止直接采用的内容 |
|---|---|---|---|
| `feature/ai-model-selector` / `origin/feature/ai-model-selector` | 当前功能最完整的 Android + `QingJi` iOS 主线 | 完整业务模型、SwiftData、AI/OAuth、预算、资产、备份、扩展、39 路由及 CI | 现有 iOS 页面结构和通用 SwiftUI 风格不能直接视为同款完成 |
| `codex/ios-import-backup-recovery`，当前审计提交 `0600683` | 基于功能主线的账务/导入/备份加固 | 报销金额归一化、导入原子保存、备份回滚和新增测试 | 不是独立产品分支，不应继续分叉长期开发 |
| `codex/ios-feimiao-batch-1-5`，已提交 `495c3f6`，另有未提交工作 | 另一套 `FeiMiao` App 重写 | 同款首页/抽屉/主题/品牌素材和部分基础 CRUD 的实现参考 | `com.feimiao.app`、`0.2.0`、iOS 18 工程、GRDB、旧 Android v40 基线、整套替换 `QingJi` |

当前主工作区 `C:\src\xunni-codex` 仍有大量 Android 用户改动和生成物，且本地分支落后远端。Luna 不得在这个脏工作区执行整体 merge、`git add -A`、整体 stash、reset、clean 或批量覆盖。

2026-09-02 现场核对时，`origin/feature/ai-model-selector` 指向 `72f14808...`，其中 Android 为 `1.285.0+299`、`QingJi` iOS 工程为 `1.286.0+300`。这些数字只是本方案复盘时的快照；Luna 开工前必须 fetch 后重新记录精确 SHA/版本/水印/DB 版本，不能把本文版本号当作永远有效的生产基线。

### 1.2 当前截图能证明什么

最新可见成对总览显示了 39 个声明场景，并且 iOS 的导入复核页已经不再错误显示首页。但它仍不能作为最终同款验收，原因包括：

- Android 和 iOS 不是同一逻辑日期：最新对比中 Android 已进入 2026 年 9 月，iOS 仍是 2026 年 8 月；
- 首页、周/月/自定义统计、预算等因此读取不同数据窗口；
- `books`、`accounts`、`reconcile`、`liabilities`、`net-worth` 中部分场景仍比较了不同入口或不同页面层级；
- 仓库并存多套 iOS 截图，`02-quickadd.png` 等页面在不同批次甚至是不同骨架；
- Android 为 1080×1920，iOS 为 1260×2736，不能用原始像素相减直接代表产品一致性；
- PNG 存在、可解码、非黑底，只证明图片有效，不证明路由、字段、操作或数据库副作用正确。

因此，当前截图适合用来发现“明显不同”，不适合用来宣称“已完成同款”。

---

## 2. “同一款 App”的强制合同

### 2.1 必须完全一致

以下内容不属于平台适配，必须与 Android 一致：

- 根信息架构：主页 + 左侧推开式抽屉，不增加五栏 Tab，不另造第二套首页；
- 每个入口的名称、位置、层级和目标页面；
- 页面首屏的信息顺序、主要卡片、主要操作和空态含义；
- 支出/收入/转账、账本、账户、分类、标签、日期、备注、附件、待报销、不计入等字段；
- 金额精度、符号、正负、净额、退款/报销归属、余额、预算、统计和净资产口径；
- Android 已有的肥喵 Logo、猫咪、分类图标、账本封面、暖色背景和语义色；
- 正常、空态、错误、取消、重复提交、删除/撤销后的可见结果；
- 设置项默认值和持久化结果；
- 同一输入产生的数据库、文件、任务或网络状态变化。

### 2.2 可以 iOS 原生化

允许不同，但不得改变产品任务的内容：

- iPhone 状态栏、Dynamic Island、安全区；
- 系统键盘、日期/时间选择器、PhotosPicker、文件选择器；
- 按压动画、触觉、侧滑返回、sheet 转场；
- VoiceOver、Dynamic Type、Reduce Motion、深色模式；
- 系统权限申请、通知和分享面板；
- iOS 无法读取第三方 App 通知、无法自替换二进制、后台定时不保证精确时刻等硬限制。

### 2.3 明确禁止

- 用 `Form` / `List` / 大标题导航把 Android 的工作台页面改成通用设置页；
- 因为“iOS 原生”而删除、合并、改名或移动 Android 入口；
- 用 SF Symbol 替代肥喵 Logo、猫咪、账本封面或分类品牌图标；
- 用一张静态页面或 mock 数据假装功能闭环；
- 用相近页面、资产资金 tab 或首页截图替代目标页面；
- 先大面积重写再补测试和截图；
- 同时维护 `QingJi` 与 `FeiMiao` 两个生产数据层和两个 Bundle ID；
- 把 CI 绿灯、PNG 数量或 IPA 生成等同于产品完成。

---

## 3. 当前差异总诊断

### 3.1 根壳层

Android 的产品身份来自“主页 + 跟手推开式抽屉 + 暖色连续背景 + 自绘圆形/胶囊控件”。当前 `QingJi` 已有抽屉，但仍更像覆盖式 iOS 面板，页面之间大量回到系统 NavigationStack、Section 和白卡列表。需要统一为同一套肥喵壳层，而不是每页各自决定风格。

### 3.2 页面骨架

最明显的差异不是字号，而是信息顺序：

- 统计：Android 以“大总额卡 + 两个摘要卡 + 洞察/环图/趋势/排行”的可管理卡片序列为中心；iOS 常改成“三个小指标 + 环图 + 系统图表”的另一套仪表盘；
- 预算：Android 是计划查看/切换/进度/分类额度的管理页；iOS 截图更接近单个“月度预算”编辑表单；
- 资产/净资产：Android 使用一个资产管理 hub 的“总览/资金/物品”层级；iOS 把账户、负债、净资产拆成多个独立系统列表；
- 设置：Android 是可关闭的半屏/全屏设置面板并保留头像、分组和业务入口；iOS 是标准 push 设置页；
- AI 设置：Android 是完整 AI 能力总入口；iOS 当前截图只突出账号与迁移，层级明显缺失。

### 3.3 组件语言

Android 已形成稳定组件语言：暖橙到奶白背景、半透明暖白卡、低饱和蓝灰主色、铜金收入、柔和橙警示、预算健康绿、48dp 圆/胶囊操作、发丝线、连续圆角、肥喵空态和分类彩色图标。

当前 iOS 虽有暖色背景，但仍大量使用系统蓝、纯白卡、标准 Toggle、默认 Section 间距和 SF Symbol，造成“品牌背景上放了另一款 iOS App”的感觉。应建立单一 token 和组件库，所有业务页只消费组件，不再各自手搓。

### 3.4 数据与状态

当前还存在同款体验无法靠视觉修复的问题：

- 逻辑日期未真正锁住，月份、持有天数、报销到账日期漂移；
- 存钱目标 6800/12000 一端显示 57%，另一端 56%，舍入规则不一致；
- AI 技能与连接的默认开关两端相反；
- 资产、净资产、预算、统计在不同 fixture 下数值不同；
- 部分路由比较了不同页面，导致“看上去不同”与“取证错误”混在一起。

视觉重建与业务对账必须并行，每批都要有字段 JSON 或 XCTest，不能只看截图。

---

## 4. 39 个现有截图槽位的差异与规范化场景

差异标签：

- **E**：证据/路由/fixture 错误，先修取证；
- **A**：信息架构或页面骨架不同，必须重构；
- **B**：业务字段、状态或计算不同，必须修复；
- **V**：主要是品牌组件和视觉细节，可在骨架正确后收口；
- **P**：确有平台硬差异，需要同目标替代并标注。

| # | 场景 | 当前差异 | Luna 的目标动作 | 主责阶段 |
|---:|---|---|---|---|
| 1 | 首页 `home-overview` | E/A/B/V：月份和交易不同；结构已接近但间距、卡片、猫、底栏仍不统一 | 先锁同月数据，再按 Android 顶栏、月汇总、猫、三段筛选、日卡、底部记一记逐锚点对齐 | P2 |
| 2 | 手动记账 `quick-add` | E/A/V：仓库中存在两种 iOS 骨架；最新 sheet 仍有尺寸/字段密度差异 | 只保留 Android 同款 sheet 骨架；同顺序展示类型、分类、chips、金额备注、附件、键盘 | P2 |
| 3 | 明细 `transactions` | A/B/V：列表密度、顶部筛选、按日卡和净额展示不同 | 复用同款按日卡；逐笔对账净额、日小计、搜索、编辑/删除/退款操作 | P2 |
| 4 | 周统计 `stats-week` | E/A/B：两端月份不同，iOS 图表顺序另造 | 固定同一周；按 Android 大总额卡、收入/结余卡、环图、每日支出、排行顺序实现 | P3 |
| 5 | 月统计 `stats-month` | E/A/B：Android 当前空月、iOS 有数据；历史同月结构仍不同 | 固定 8 月；按 Android 月卡序列、洞察、预算、图表和排行实现 | P3 |
| 6 | 年统计 `stats-year` | A/B：Android 大趋势卡 + 摘要 + 构成，iOS 改成小指标 + 月柱 + 排行 | 统一 12 月序列、卡片顺序、分类构成和排行 | P3 |
| 7 | 自定义统计 `stats-custom` | E/A/B：日期漂移、空态和结果不同 | 统一起止日含整天、记忆上次范围，并复用统计卡片序列 | P3 |
| 8 | 预算 `budget` | E/A/B：Android 是预算管理页，iOS 是单计划编辑页 | 首页进入预算管理总览；编辑作为二级 sheet；补版本、周期、分类额度、固定承诺和专项追踪 | P3 |
| 9 | 对账 `reconcile` | E/A/B：Android 截图实际是资产资金页，iOS 是对账页 | 新增真实 Android 对账场景；iOS 从账户详情进入同款校准 sheet，验证撤销与余额 | P4 |
| 10 | 待报销 `reimburse` | B/V：整体接近，卡片和状态密度不同 | 对齐待处理总额、原单日期、分类、已报销状态和到账入口 | P2 |
| 11 | 存钱目标 `savings` | B/V：56%/57% 舍入不同，卡片操作不同 | 统一舍入规则、目标卡、存入/取出、归档和资产关联 | P3 |
| 12 | 定时记账 `recurring` | B/V/P：卡片文案、下次日期和后台能力需核对 | 同规则/下次日期/幂等物化；iOS 后台差异用提醒+前台补算 | P3 |
| 13 | 资产 `assets` | A/B/V：iOS 增加 Android 没有的净资产摘要层 | 按 Android 资产 hub 的总览/资金/物品层级；不在 iOS 单独增加首屏层 | P4 |
| 14 | 负债 `liabilities` | E/A/B：Android 证据是资金页，iOS 是独立负债页 | 捕获 Android 真入口；在资产 hub 中保持同层级，完成建档/还款/本息 | P4 |
| 15 | 净资产 `net-worth` | E/A/B：数值和页面层级不同 | 使用 Android 总览结构，逐项对账资金、投资、物品、权益、负债、质量状态和趋势 | P4 |
| 16a | 抽屉账本快切 `drawer-books` | E/A/V：原 `books` Android 图实际是抽屉账本区 | 废弃含糊的 `books` 对比 ID；对齐抽屉账本列表、快切、星标、排序和新建入口 | P1 |
| 16b | 账本管理 `books-management` | E/A/V：原 `books` iOS 图实际是管理列表 | 新增 Android 同入口基准；对齐列表、封面/备注、归档、删除迁移和总账本保护 | P2 |
| 17a | 账户管理 `accounts-management` | E/A/B：原 `accounts` iOS 图实际是管理列表 | 新增 Android 同入口基准；对齐账户 CRUD、类型、归档、期初余额和记账选择 | P2 |
| 17b | 资产资金 `assets-funds` | E/A/B：原 `accounts` Android 图实际是资产资金 tab | 废弃含糊的 `accounts` 对比 ID；按资产 hub 资金 tab 验收余额、分组和详情入口 | P4 |
| 18 | 分类 `categories` | B/V：整体接近，但层级、行高、图标和操作不同 | 120 个稳定 key/父子关系先对账，再统一列表/展开/更多菜单 | P2 |
| 19 | 标签 `tags` | B/V：空态和操作位置不同 | 对齐空态猫、名称/颜色/排序、新增编辑删除和历史引用 | P2 |
| 20 | 设置 `settings` | A/B/V：Android 面板与 iOS push 设置页是两套结构 | 重建为 Android 同款头像、管理、显示、提醒、小组件、关于分组；原生控件只替换内部交互 | P3 |
| 21 | AI/Chats `ai` | A/B/V：会话卡、顶栏、搜索、新聊天和品牌空态不同 | 对齐 Chats 信息层级和入口；消息页再对账流式、附件、来源、记账卡、撤销 | P5 |
| 22 | AI 设置 `ai-settings` | A/B：iOS 缺少 Android AI 总入口层级 | 按 Android 顺序恢复账号、用途、隐私、任务、诊断、搜索、记忆、技能、定时报表、本地模型 | P5 |
| 23 | 导入复核 `import-review` | A/B/V：路由已修，但摘要、商户组、退款和底部动作仍不同 | 对齐来源统计、账户选择、商户分组、逐行分类、退款配对和原子导入结果 | P3 |
| 24 | 报告库 `reports-library` | A/B/V：Android 半屏报告库，iOS push 列表，摘要字段不同 | 保留 Android 报告库层级；统一标题、期间、收支结余、状态、阅读、置顶、删除；`ai-report-generation` 另建 P5 操作合同 | P3 |
| 25 | 分类记忆 `memory` | A/B/V：空态品牌、列表和删除语义不同 | 使用肥喵空态；对账商户→分类映射和忘记后历史不变；AI 可控记忆使用独立 routeId | P2 |
| 26 | AI 任务 `ai-tasks` | B/V：状态卡和空态不同 | 对齐阶段、状态、时间、配置摘要、取消/重试/恢复和脱敏错误 | P5 |
| 27 | AI 诊断 `ai-diagnostics` | B/V：Android provider 卡，iOS 运行记录分组 | 统一 provider、次数、耗时、失败和脱敏摘要；空态也同义 | P5 |
| 28 | AI 搜索 `ai-search` | A/B/V：Android 顶部搜索，iOS 底部搜索 | 按 Android 搜索框位置、范围和结果分组；键盘用 iOS 原生 | P5 |
| 29 | AI 记忆 `ai-memory` | A/B/V：开关、授权说明和删除入口层级不同 | 对齐授权范围、记忆列表、删除、上下文使用和隐私说明 | P5 |
| 30 | 技能与连接 `ai-extensions` | B/V：Android 默认关，iOS 默认开 | 统一默认状态、白名单和真实行为；按 Android 分组和顺序 | P5 |
| 31 | AI 定时报表 `ai-schedules` | B/V/P：数据需一致，后台时机不同 | 对齐计划/时区/启用/内容；iOS 使用通知提醒打开 App，不伪装精确定时 | P5 |
| 32 | 本地模型 `ai-local` | B/V/P：地址、健康状态和安全说明需对账 | 统一回环地址、健康检查和仅本机明文 HTTP 策略 | P5 |
| 33 | 备份 `backup` | A/B/V：Android 是功能卡片，iOS 是简化行列表 | 对齐完整备份、文件恢复、本机恢复点、附件、校验失败和回滚；文件选择器原生 | P3 |
| 34 | 主题 `theme` | A/B/V：iOS 缺 Android 六套色卡、背景浓度、卡片透明度 | 将 Android 主题枚举和滑杆完整映射到 iOS token；Liquid Glass 只作为材质实现 | P3 |
| 35 | 显示 `display` | A/B/V：Android sheet 与 iOS push 页不同，示例区不一致 | 对齐内容/分类优先、聊天气泡、实时预览和持久化效果 | P3 |
| 36 | 收入记账 `quick-add-income` | A/B/V：分类集、金额语义色、sheet 尺寸需核对 | 与支出共用同一骨架，只切换收入分类和铜金语义；保存结果对账 | P2 |
| 37 | 物品详情 `asset-detail` | E/A/B/V：日期导致持有指标不同，操作顺序不同 | 固定 as-of；按 Android 图片/成本/持有指标/档案/操作顺序，补关联账单和退款分摊 | P4 |
| 38 | 账户详情 `account-detail` | A/B/V：摘要、校准、趋势、资料、活动顺序不同 | 按 Android 页面顺序；逐项对账余额、趋势序列、活动和校准副作用 | P4 |
| 39 | 报销到账 `reimburse-settlement` | E/B/V：到账日期不同，sheet 控件不同 | 固定到账日期；对齐金额、账户、日期、确认语义和到账后的原单净额 | P2 |

原有 39 个截图槽位在拆分 `books/accounts` 后形成至少 41 个唯一页面合同，仍不够覆盖完成态。后续必须新增：转账保存、编辑账单、部分退款、全额退款、删除原单级联、预算承诺匹配/跳过、备份校验失败/回滚、AI 提案取消/保存/撤销、权限拒绝、深色模式和大字体。

表中的“主责阶段”只表示实现和阶段关闭责任。P0 负责为所有场景建立可信证据合同，P1 提供公共壳层/组件，P6 对所有场景和新增操作链统一复验；这不构成多头负责。

---

## 5. 目标工程架构

### 5.1 生产主干

继续使用：

- `ios-app/QingJi`：唯一 App target；
- `ios-app/QingJiCore`：共享纯规则；
- `ios-app/QingJiTests`：App/SwiftData/网络/恢复测试；
- `ios-app/QingJiWidget`、`ios-app/QingJiShare`：系统扩展；
- Bundle ID `com.qingji.app` 及其扩展 ID；
- 当前 canonical 备份格式、Keychain、SwiftData 模型和版本迁移。

### 5.2 从 `FeiMiao` 分支可移植的内容

每一项先与当前 Android（不是 1.197 旧基线）复核，再移植到 `QingJi`：

- `FeiMiaoTheme` 中与 Android token 一致的颜色、圆角、间距和透明度；
- `SideDrawerView` 的推开交互、主页缩放/圆角/阴影；
- `HomeSummaryCard`、`HomeTransactionFilterControl`、`TransactionDayCard`、`HomeRecordInputBar`；
- Logo、猫咪、账本封面资源；
- `ParityGlassSurface`、按压/触觉样式和 44/48pt 热区处理；
- 可证明与 Android 当前字段一致的空态和列表排版。

### 5.3 不得移植

- `FeiMiao/App/AppStore.swift`；
- `FeiMiaoKit/FeiMiaoData`、GRDB schema 和仓储；
- `FeiMiaoKit/AndroidBackupImporter`；
- `FeiMiao/project.yml`、`com.feimiao.app`、版本 `0.2.0`；
- 旧 Android v40 的页面/功能假设；
- 任何会丢失现有 AI、预算、资产、扩展、备份或迁移能力的重写。

### 5.4 分支整合策略

- 在 fetch 并确认用户认可的 Android 母版后，从其对应的功能完整 iOS 主线创建干净 `codex/ios-same-app`（或同义）integration worktree；所有 P0–P6 提交进入这一条版本线；
- 不把当前脏主工作区、分叉的 `origin/main` 或完整 `codex/ios-feimiao-batch-1-5` 作为合并起点；
- `0600683` 作为一个独立候选变更审计：测试和数据合同成立才 cherry-pick/重做，不把整个审计分支继续长期分叉；
- `FeiMiao` 只允许逐文件移植纯 UI/资产；移植文件不得引用 `FeiMiaoDomain`、`FeiMiaoData`、GRDB 或第二个 `AppStore`；
- `FeiMiao` worktree 中的未提交文件视为未审计个人工作，不直接消费；先由原作者提交或导出精确 diff，再决定采用；
- 每批提交必须可独立回退；禁止跨阶段大提交和“先整分支 merge 再修”。

### 5.5 建议的 UI 分层

在 `QingJi` 内建立统一层次，避免业务页直接散落系统默认样式：

```text
QingJi/
├── AppShell/                 # 根壳、推开式抽屉、全局路由、背景
├── DesignSystem/             # token、品牌资产、按钮、卡片、sheet、设置组
├── Components/
│   ├── Ledger/               # 月汇总、按日卡、账单行、徽章
│   ├── Entry/                # 分类网格、chips、金额卡、键盘
│   ├── EmptyState/           # 肥喵空态
│   └── Charts/               # 环图、趋势、排行
├── Views/                    # 只组合组件并绑定业务状态
└── Models/Stores/            # 保持现有 SwiftData/Store 能力
```

视图不得直接重复定义背景色、圆角、卡片透明度、返回按钮、sheet header、设置 row 和空态猫。

---

## 6. P0–P6 执行计划

## P0：锁定唯一产品基线和证据链

### 目标

在写 UI 前消除“版本、页面、日期和数据都不相同”的干扰，确定唯一生产主干与唯一 Android 母版。

### P0 执行产物（2026-09-02）

- 基线决策与当前证据：`ios-app/docs/p0/P0_BASELINE_DECISIONS.md`；
- 41 场景、12 旅程和系统能力机器合同：`ios-app/tools/p0_product_contract.json`；
- 固定输入：`ios-app/tools/fixtures/p0-demo-ledger-2026-08-v1.json`；
- 自动门禁：`ios-app/tools/check_p0_product_contract.py`；截图 provenance：`ios-app/tools/write_parity_metadata.py`、`ios-app/tools/check_capture_metadata.py`。

当前状态必须保持 `P0_PARTIAL`，直到报告中的 6 个开放门全部关闭；合同结构通过不等于 P0 完成。

### P0.1 工作区和分支收口

1. fetch 后从最新经用户认可的 Android 生产提交及其对应的功能完整 `QingJi` 起点创建干净 integration worktree，记录完整 SHA、分支、Android/iOS 版本、水印、DB 版本；2026-09-02 的 `72f14808... / Android 1.285.0+299 / iOS 1.286.0+300` 仅供发现漂移，不能代替现场确认。
2. 以功能完整的 `QingJi` 主线为 iOS 起点；把 `0600683` 中仍未进入主线的账务/导入/备份修复作为候选逐提交审计。
3. 对 `codex/ios-feimiao-batch-1-5` 只做文件级清单，不 merge 整个分支；记录“采用/重写/拒绝”三类。
4. 冻结 `FeiMiao` 第二 target 的扩张；在整合完成前不删除，保留回退和参考。
5. 建立唯一 integration 分支和 worktree 路径；后续每批只从该分支继续，禁止在脏主工作区直接开发。

### P0.2 页面合同

先从 Android 当前导航、抽屉、设置、sheet、长按/滑动菜单、深链和系统入口生成完整功能/路由清单，再与 iOS 路由和 Store 能力逐项映射。39 个旧截图槽位只是种子证据，不是产品范围上限；任何 Android 可达功能没有合同，都不得宣称盘点完成。

为每个场景建立机器可读合同，至少包含：

```json
{
  "routeId": "home-overview",
  "androidEntry": "root/home",
  "iosEntry": "root/home",
  "requiredAnchors": ["drawer", "search", "book", "month", "summary", "filter", "transactions", "recordBar"],
  "requiredFields": ["income", "expense", "balance", "budget", "todayAllowance"],
  "allowedPlatformDifferences": ["statusBar", "safeArea", "haptics"],
  "forbiddenSubstitutions": []
}
```

对 `books/accounts/reconcile/liabilities/net-worth` 拆分错误混用的场景，禁止一图多义；旧 `books/accounts` 截图槽位仅作来源索引，规范合同改为 `drawer-books`、`books-management`、`accounts-management`、`assets-funds`。

### P0.3 固定 fixture

- baseline：`p0-demo-ledger-2026-08-v1`；
- now：`2026-08-27T12:00:00+08:00`；
- locale：`zh-Hans`；timezone：`Asia/Shanghai`；currency：`CNY`；book：`总账本`；
- 预期收入 `620.00`、支出 `1017.90`、结余 `-397.90`、预算 `3000.00`；
- 所有相对日期、持有天数、报销到账和定时规则均从注入时钟读取；生产构建仍使用真实时间；
- 两端加载同一份 fixture input hash，并都能导出字段 JSON；Android 输出冻结为期望值，iOS 当前输出保留为差异证据，具体业务差异由 P2–P4 的唯一主责阶段关闭。

### P0.4 证据门禁

- 每张原图带 source revision、版本、fixtureId/hash、routeId、设备、OS、locale、timezone；
- Android 1080×1920、iOS 1260×2736；alpha、非空、近重复和页面 anchor 均检查；
- 对比前按内容宽度归一化，状态栏和安全区单独标记，不做裸像素等尺寸相减；
- 原图不可覆盖；按提交/批次存档；
- 原有 39 个 Android 截图槽位全部重采并映射到规范 routeId；拆分后至少 41 个规范页面合同分别取证，再增加关键操作后状态。iOS 尚未实现的页面记录 `missing`，不得用相近页面或假页面补数。

### P0.5 产品、数据和分发决策

P0 必须把以下决定写入机器可读 baseline，后续阶段不得自行改口：

- **唯一产品目标**：`QingJi` App + Widget + Share Extension + App Intents，Bundle ID 保持 `com.qingji.app` 体系；
- **系统支持矩阵**：当前成熟工程为 iOS 26.0，`FeiMiao` 供体为 iOS 18.0；默认保持成熟工程的 iOS 26.0，不得因移植供体文件静默改变 deployment target。若用户决定扩大支持范围，必须先做 API 可用性、CI 设备矩阵和真机成本评估；
- **存储与 iCloud**：默认保持 Android 对等的本地优先、无静默云同步。若保留可选 iCloud，必须明确 entitlement、开关默认值、冲突/删除/多设备语义和断网测试；未启用 entitlement 时 UI 不得宣称已自动同步；
- **Android 数据输入**：记录母版实际 DB 版本；以当前版本真实备份为主样本，并保留 v40、v48 及仓库现存历史样本做向后兼容，不写死“v48+”替代现场版本；
- **已有 iOS 用户数据**：建立当前 `QingJi` SwiftData 版本到目标版本的升级 fixture，视觉重构不得换库或丢数据；每次 schema 变化必须有升级、失败回滚和附件完整性测试；
- **分发目标**：明确本轮是开发侧载、TestFlight 还是 App Store 可发布。若目标是真正交付，P6 必须通过签名、隐私清单、权限说明、归档、升级和回退，而不只生成 unsigned IPA。

### P0 关闭门

- 两端同一提交族、同一 fixture、同一逻辑日期；
- 原有 39 个截图槽位全部映射完毕，拆分后的全部规范 routeId 都有唯一页面合同；目标页已存在时必须解析到正确页面，尚未实现时明确标记 `missing` 和唯一主责阶段，禁止用相近页面代替；
- 全部 Android 规范基准图和已存在的 iOS 改前图都有完整 metadata；缺失 iOS 页面用合同状态记录，不生成假截图；
- 固定时钟和 fixture input hash 完全一致；Android 的基础交易列表、月收入、月支出、月结余、账户余额、退款净额和预算基数已冻结，iOS 对应 JSON 已完整捕获并生成逐字段差异，差异进入 P2–P4 的唯一主责阶段；
- `books/accounts/reconcile/liabilities/net-worth` 已拆成正确 routeId，不再一图多义；
- 分支、deployment target、存储/iCloud、数据升级和分发目标均已锁定；
- 输出 P0 复盘报告；本阶段证据缺陷必须为 0，P1–P5 的已登记实现差异允许保留但必须有唯一主责阶段。

---

## P1：统一产品壳层、品牌和公共组件

### 目标

先消除“另一款 App”的视觉和导航根因，让任何后续页面天然属于肥喵，而不是继续出现系统默认页面孤岛。

### P1.1 设计 token 与资产

- 先将 Android 默认暖色主题完整映射为 iOS token，并把 token API 设计为可承载简约白/樱粉/薄荷/雾蓝/暮夜；六套主题的设置 UI、切换和持久化归 P3，避免壳层阶段被设置功能拖住；
- 对齐背景渐变、卡片 alpha、选中态 alpha、发丝线、圆角、阴影和 8pt 间距节奏；
- 收入使用铜金、支出使用正文中性色、警示使用柔和橙、预算健康使用绿；不得默认改成系统蓝/红；
- 同步 Logo、猫咪、当前 Android 分类 manifest 中的全部图标和账本封面，校验文件 hash、稳定 key、实际数量和显示裁切；不得把历史记录的 120 当作永远不变的数量；
- 支持深色模式，但不改变语义色。

### P1.2 根壳和抽屉

- 实现与 Android 一致的跟手推开式抽屉：主页整体右移、圆角、阴影、遮罩和边缘手势；
- 抽屉顺序：品牌头、可排序功能区、更多/收起、我的账本、账本列表、底部新建账本和设置；
- 账本快切、加星、编辑、排序、总账本保护和封面必须绑定 `QingJi` 现有数据；
- 全局仍由一个路由器管理，不创建底部 Tab 或第二套 NavigationStack 产品层。

### P1.3 公共组件

实现最小可复用集合，并在首页壳层/组件画廊验证；不得为了“统一组件”一次性改遍尚未进入主责阶段的业务页：

- `AppCircleButton` / `AppBackButton` / `AppPillButton`；
- `SheetHeader` / `SettingsGroup` / `SettingsRow` / `SettingsSectionLabel`；
- 暖色卡片、分段胶囊、肥喵空态、账本胶囊、分类图标；
- 月汇总卡、按日卡、账单行、状态徽章、底部记一记输入卡的基础视觉外壳，实际字段和操作在 P2 接线；
- 图表卡片、进度条、提示条和危险操作的基础材质；实际统计数据和交互在 P3 接线；
- 所有按钮至少 44pt iOS 热区，视觉尺寸与 Android 接近。

### P1.4 路由呈现和组件画廊

- 锁定 push、sheet、全屏和抽屉入口的呈现合同，防止同一页面在不同路径出现两种骨架；
- 建立非生产组件画廊，覆盖默认主题浅/深色、长文案、空态、错误态和 200% 字体；
- 组件不得自建 demo Store 或第二套导航；画廊数据与生产数据隔离。

### P1 小批次顺序

1. token + 品牌资产；
2. 根壳 + 抽屉；
3. 按钮/卡片/sheet/设置组 + 路由呈现合同 + 组件画廊。

每小批次都要出 Android 基准、iOS 改前、iOS 改后和三图并排，不允许四批做完后才第一次看截图。

### P1 关闭门

- App 内不存在生产五栏 Tab、通用大标题首页、SF Symbol 猫；
- 首页壳层、抽屉和公共组件使用同一 token；
- 默认主题的浅/深色可用，其他五套主题已有 token 映射但其切换/持久化验收归 P3；
- 小/大 iPhone、100%/200% 字体无溢出；
- VoiceOver 顺序、Reduce Motion 和边缘返回不破坏抽屉；
- P1 涉及的壳层和公共组件不得遗留重复实现；尚未进入主责阶段的页面不得被大面积机械替换。

---

## P2：核心账务同款

### P2.1 首页

- 顶栏：抽屉、搜索、账本快切；
- 月份选择与统计入口；
- 月预算剩余/超支、收入、支出、今日可用环；
- 全部/支出/收入分段；
- 按日账单卡、日小计、分类/备注显示策略、退款/报销/不计入徽章；
- 底部记一记卡，手动/AI 模式和键盘避让；
- 空态猫和文案。

P2 只对齐首页现有预算摘要的读取和展示；预算版本、承诺、专项追踪等高级语义由 P3 实现。P3 若改变首页共享结果，相关首页回归归 P3 本批负责，不得推回 P2。

### P2.2 记一笔

- 支出/收入/转账在同一页面切换；
- 表达式金额、两级分类、常用二级排序；
- 日期、账本、账户、标签、待报销、不计入；
- 备注、照片、相册；
- 再记/完成、取消、保存失败；
- 转账同账户/不同币种拒绝，转账不进入收支统计；
- 所有保存走现有 `LedgerStore` 原子 API，不在 View 里拼散数据。

### P2.3 明细、搜索、编辑和删除

- 首页和搜索复用同一按日卡；
- 搜索覆盖内容、分类、账户、账本、标签、金额和日期；
- 编辑保持退款家族约束；
- 删除原单级联删除 offset，删除报销子行恢复待报销状态；
- 附件只在数据库提交成功后清理；
- swipe actions 与 Android 语义一致，交互形式可原生。

### P2.4 退款与报销

- 退款/报销必须附着原单，冲减日期归属原单日期，到账日期单独记录；
- 部分和全部金额四舍五入到分，不能超过剩余；
- `59.999` 等输入按归一化金额判断是否完全报销；
- 原单净额、badge、待报销列表、结算账户、删除/撤销一致；
- 找不到原单的导入退款按可见收入处理，不静默丢失。

### P2.5 账本、账户、分类、标签、分类记忆

- 账本：总账本保护、封面/备注、星标、排序、归档、删除迁移；
- 账户：类型、期初余额、信用卡字段、归档、重名 UUID、流水；
- 分类：以 P0 锁定的 Android 分类 manifest 为准，对齐全部稳定 key、两级树、自定义、隐藏、合并；
- 标签：增删改、排序、历史快照；
- 分类记忆：决定性商户学习、平台名不误学、忘记不改历史。

本节账户范围是账户 CRUD、基础余额和记账选择；资产资金 hub、账户详情趋势、校准和净资产联动归 P4。

### P2 小批次顺序

1. 首页；
2. 支出/收入记账；
3. 转账 + 明细/搜索；
4. 编辑/删除 + 退款；
5. 报销到账；
6. 账本/账户；
7. 分类/标签/分类记忆。

### P2 关闭门

- 同一 fixture 的交易 JSON、月汇总、账户余额、退款状态完全一致；
- 正常、空、取消、失败、重复提交均有测试；
- 每条用户操作都有数据库副作用断言；
- 上述页面成对截图达到同入口、同骨架、同字段；
- P2 范围内的视觉欠账、业务 TODO 和回归必须为 0；仅允许保留已在阶段表中明确归 P3–P6 的计划项。

---

## P3：统计、预算、计划、导入备份与报告

### P3.1 统计

- 周/月/年/自定义使用 Android 同一四段控件；
- 同一时间窗口和空白日补齐规则；
- 卡片顺序对齐 Android：总额/摘要、洞察、预算、构成、趋势、分类排行、单笔排行；
- 月统计保留卡片管理/排序/删除/添加；
- 图表可用 Swift Charts，但数据点、颜色、图例、标签和交互语义一致。

### P3.2 预算

- 预算管理总览与单计划编辑分层，不把编辑页当首页；
- 月/周/自定义周期、账本、总预算、分类额度；
- 计划版本、历史修订、生效范围；
- 固定承诺模板、当前/下一周期 occurrence、匹配/跳过/退款复核；
- 专项追踪、今日可花、历史月份和退款/报销净额。

### P3.3 存钱目标和定时记账

- 存入/取出、进度舍入、归档/恢复、关联资产；
- 日/周/月/年、转账、结束日期/次数、账户失效；
- 重复打开不重复落账；
- iOS 后台不能保证准点时，用本地通知和前台补算实现同目标。

P3 关闭定时规则、下次日期、幂等物化和通知 hand-off 合同；P5 负责 UserNotifications/BGTask 的真实系统接线和真机验证。P3 不得用假的“通知成功”状态冒充系统接线。

### P3.4 导入和备份

- 微信/支付宝/CSV/XLSX、GBK/UTF 编码和真实导出文件；
- 商品优先分类、商户组、账户、重复指纹、0 元/不计入、退款订单号；
- 批量保存原子性：任何 save 失败均 rollback，本批不残留；
- canonical ZIP、P0 锁定的当前 Android DB 原始 SQLite ZIP、v40/v48 等历史 fixture、附件和 manifest/SHA-256；
- 完整替换与显式合并模式；
- 校验失败、附件失败和保存失败均恢复旧数据与旧附件；
- 本机恢复点和最多保留策略。

### P3.5 报告、设置、主题与显示

- 报告库、标题、期间、收入、支出、结余、生成状态；
- 阅读、置顶、删除、后台限制说明；
- 设置页按 Android 恢复头像、管理、显示、提醒、小组件、关于的分组和 sheet 层级；
- 补全暖橙/简约白/樱粉/薄荷/雾蓝/暮夜六套色卡、背景浓度、卡片透明度、浅/深色切换和持久化；
- 显示页补全内容/分类优先、聊天气泡、实时预览、金额显示和持久化效果；
- 还款提醒、小组件隐藏金额和关于信息；
- iOS 文件选择器/通知开关保持原生，但入口与 Android 同层。

P3 负责 `reports-library` 的数据模型、列表、阅读、置顶和删除；真实 AI 报告生成、任务恢复、模型调用和错误重试归 P5 的 `ai-report-generation` 合同。P3 的生产 UI 必须诚实显示当前能力，不能用 mock 报告假装生成完成。

### P3 关闭门

- 四种统计窗口逐字段 JSON 一致；
- 预算操作链和 occurrence 有真实副作用测试；
- 真实导入样本覆盖重复、匹配退款、未匹配退款、失败回滚；
- 完整 ZIP、附件、校验失败和失败恢复测试通过；
- 统计/预算/导入/备份/报告/设置/六套主题/显示均有操作后截图；
- P3 自有合同内无“页面有了但操作未接线”；通知系统接线和 AI 报告生成按上述独立合同由 P5 负责，不得在 P3 UI 中伪装已完成。

---

## P4：资产、负债、对账和净资产

### P4.1 资产管理 hub

- 按 Android 恢复“总览 / 资金 / 物品”顶层；
- 搜索、筛选、添加和更多入口同层；
- iOS 不额外插入 Android 没有的首屏摘要卡。

### P4.2 资金、账户详情与对账

- 现金、储蓄卡、信用卡等分组和余额；
- 账户详情按 Android 顺序：余额、校准、趋势、资料、最近活动；
- 对账为账户详情内的校准操作，不生成普通收入/支出；
- 校准后余额、撤销、历史和净资产同步。

### P4.3 物品资产

- 档案、购置、当前估值、照片、状态；
- 持有天数、日均成本、每次成本、保值率；
- 更新估值、关联支出、记录使用、出售、退货、报废、丢失、赠送；
- 资产成本自动关联和退款分摊；
- as-of 日期固定，避免持有指标漂移。

### P4.4 权益、应收、负债和还款

- 应收、部分收回、损失、归档/恢复；
- 信用卡、房贷、车贷、个人借入；
- 本金/利息拆分、账户余额、分期向导、还款提醒；
- 负债不得在净资产中重复计算。

### P4.5 净资产

- 资金、投资、物品、权益、负债、外币质量状态；
- 快照、历史趋势、校准后的结果；
- Android 总览页面结构和字段顺序是母版。

### P4 关闭门

- 每类资产至少一条新增→变更→结束状态链；
- 账户校准可撤销，净资产同步；
- 本息、回收和退款分摊逐字段一致；
- 资产 hub、物品详情、账户详情、负债、净资产截图同骨架；
- P4 复盘不得保留错误页面替代或重复计算。

---

## P5：AI、OAuth 与 iOS 系统能力

### P5.1 AI 信息架构

按 Android AI 设置顺序完整恢复：

1. AI 账号设置；
2. 用途分配；
3. 隐私与数据；
4. AI 任务中心；
5. AI 诊断；
6. 统一搜索；
7. 可控记忆；
8. 技能与连接；
9. 定时报表；
10. 本地模型伴侣。

不得把这些入口隐藏在账号迁移页中。

### P5.2 Chats 和 AI 记账

- Chats 列表、新聊天、搜索、会话恢复；
- 流式响应、停止、超时、错误和重试；
- 图片/文件附件、来源、处理摘要、Markdown/表格；
- 结构化多笔提案，确认前不落库；
- 改分类、批量原子保存、撤销、删除单条；
- AI 不读取未经授权的账本内容，错误和 run 脱敏。

同时关闭 `ai-report-generation`：从报告库发起生成、真实模型运行、前后台任务交接、完成/失败/重试/恢复和最终报告入库；不得只验证报告列表。

### P5.3 账号、模型和真实网络

- Keychain、多服务商、多账号、多模型、端点和 Effort；
- 同名模型按账号隔离，重启保持；
- ChatGPT/Codex PKCE/state、loopback、刷新和模型目录；
- Responses/Claude/兼容端点真实请求；
- 不把模拟响应或只拉模型列表当作连接成功。

### P5.4 iOS 系统能力

- 四类 Widget；
- App Intents / Siri / 快捷指令；
- Share Extension 文本和单图；
- Vision OCR、中文 Speech；
- UserNotifications、BGTaskScheduler；
- 权限拒绝、再次授权、冷启动 hand-off；
- App Group 快照和隐私隐藏金额。

### P5.5 平台边界

| Android 能力 | iOS 同目标方案 | 验收口径 |
|---|---|---|
| 全局读取微信/支付宝通知 | 分享扩展、OCR、快捷指令、用户主动自动化 | 明确说明不支持系统级监听；用户仍能快速进入复核/记账 |
| WorkManager 尽量后台生成 | 本地通知提醒 + 打开 App 前台补算，BGTask 仅机会性刷新 | 不承诺固定时刻完成 |
| APK 应用内更新 | TestFlight/App Store/侧载重签 | 不提供假更新按钮 |
| 任意明文代理/本地服务 | 仅回环或受控私网，远程使用 HTTPS | 安全策略和健康状态一致 |

### P5 关闭门

- AI 10 个入口、默认开关和状态一致；
- 每个真实 provider 至少完成登录/配置→模型目录→最小 ping→真实响应；
- 提案取消不落账，保存和撤销有数据库断言；
- Widget/Share/App Intent/OCR/Speech/通知至少在一台受支持真机完成安装与冒烟；P6 再覆盖完整设备、权限、冷启动和升级矩阵；
- 平台差异清单准确，不伪装 Android 权限；
- P5 范围内不得留下“已实现但从未运行”的系统能力；仅性能、长期后台时机和多设备矩阵进入 P6。

---

## P6：全量回归、无障碍、性能和发布交付

### P6.1 全量同款矩阵

每个功能域必须同时具备：

1. 同 routeId、同 fixture 的 Android/iOS 原图；
2. 同一输入的字段 JSON 或逐字段测试；
3. 正常、空态、错误/取消和操作后状态；
4. 数据库/文件/任务/网络真实副作用；
5. 明确的允许平台差异；
6. 无未解释的路由替代、数值差异和默认状态差异。

### P6.2 黄金用户旅程

必须在干净安装、已有 iOS 数据升级和 Android 备份恢复三种起点下，至少完成以下端到端旅程；每条都记录界面、业务 JSON 和真实副作用：

1. 首次启动 → 空态 → 新建/选择账本 → 记第一笔支出；
2. 恢复当前 Android 完整备份 → 核对账本/账户/分类/标签/附件/金额 → 继续记账；
3. 支出、收入、转账 → 编辑 → 删除/撤销，统计和账户余额同步；
4. 部分退款 → 再次退款 → 全额退款，以及待报销 → 部分/全部到账；
5. 新建预算/承诺/专项追踪 → 周期切换 → 匹配/跳过 → 首页和统计同步；
6. 新建定时记账 → 前台补算 → 重复打开不重复落账；
7. 新建账户 → 校准/撤销 → 账户详情和净资产同步；
8. 新建物品资产 → 估值/使用 → 出售或退货 → 关联账单和净资产同步；
9. AI 账号/OAuth → 模型目录/ping → 对话 → 多笔记账提案取消、保存、撤销；
10. 分享文本/图片或 OCR → 导入复核 → 原子保存；Widget/App Intent/通知从冷启动进入正确页面；
11. 从 P0 锁定的旧 `QingJi` 数据版本升级 → 全部数据和附件仍在 → 新版本继续写入 → 备份可恢复；
12. iCloud 若在 P0 选择启用：两设备创建/编辑/删除冲突、断网恢复和退出账号；若未启用，所有同步文案和入口必须真实反映本地模式。

任一旅程只能靠 mock、静态截图或手工改数据库通过，均视为失败。

### P6.3 设备与可访问性

- 小屏 iPhone、iPhone Air、最大屏；
- 浅色/深色和六套主题；
- Dynamic Type 100%/200%；
- VoiceOver 顺序、标签、状态；
- Reduce Motion、提高对比度；
- 中文输入法、键盘遮挡、安全区和旋转限制；
- 照片、麦克风、通知、网络权限拒绝和恢复。

### P6.4 性能与可靠性

- 冷启动、10k 流水首页、统计滚动和搜索；
- 重复进入页面不重复物化/导入/生成；
- 崩溃恢复、低内存、网络超时；
- 备份恢复、升级迁移、附件完整性；
- Widget/Share/Intent 冷启动和 App Group 一致性。

### P6.5 构建、隐私与交付

- XcodeGen、QingJiCore tests、QingJiTests、Simulator build；
- Release device build、签名 Archive、完整 IPA、无扩展 IPA；unsigned IPA 只能作为补充测试产物，不能冒充可交付版本；
- Bundle ID、版本、deployment target、签名/Provisioning、entitlements、App Group、可选 iCloud 和扩展内容；
- `PrivacyInfo.xcprivacy`、所有权限 usage description、App Privacy 数据标签、加密/export compliance、第三方 SDK 隐私声明；
- 按 P0 的分发目标完成开发侧载、TestFlight 或 App Store 验证；若目标为 TestFlight/App Store，必须实际上传并通过处理，不能只做本地 archive；
- IPA SHA-256、源提交、构建日志和回退提交；
- Personal Team 7 天续签说明；
- 用户 iPhone 真机安装、冷启动、旧版升级、Android 恢复、Widget、分享、OAuth、网络和回调；
- `FeiMiao` 不再进入生产 project/archive/signing；保留可回退 tag 后，可作为独立历史参考归档，生产只保留 `QingJi` 一条数据和版本线。

### P6 最终关闭门

- 所有 P0–P5 关闭门再次审计通过；
- 任何“未运行/未验证”都不能写成完成；
- 当前生产只剩一个 iOS App target、一个数据真相和一条版本线；
- 12 条黄金用户旅程全部通过，旧 iOS 数据升级和当前 Android 备份恢复均无丢失；
- P0 选定的分发方式已真实安装/上传验证，隐私、签名、权限和 entitlement 与产品文案一致；
- 用户看到的主路径和 Android 是同一款肥喵；
- 只有完成真机验收和可回退交付后才标记项目完成。

---

## 7. Luna 每个小批次的固定工作法

Luna 每次只执行一个小批次，必须按以下顺序：

### 7.1 开工前

1. 读取 `AGENTS.md`、本方案、阶段合同和上一批复盘；
2. 检查精确分支、HEAD、工作区状态和并行 worktree；
3. 读取 Android 目标页面及其直接依赖组件；
4. 读取 `QingJi` 对应页面、Store 和测试；
5. 查看同提交、同 fixture 的 Android 原图和 iOS 改前图；
6. 写出本批“必须相同 / 允许不同 / 不在本批”三栏；
7. 若截图或路由证据错误，先回到 P0 修证据，不能凭猜测开发。

### 7.2 实施中

- 先改公共组件，再改页面；
- View 只绑定现有 Store，不另建第二套数据模型；
- Android 合同需要现有 `QingJi` 缺失字段/状态时，在 `QingJiCore`/SwiftData/现有 Store 中增量实现并提供迁移测试；“不建第二套模型”不等于禁止完善唯一模型；
- 不修改稳定分类 key、UUID、退款家族、备份 schema 和 OAuth 安全边界来迁就 UI；
- 不使用假数据填充生产页面；demo fixture 只在明确的截图环境开启；
- 不触碰 Android 无关改动；
- 不做整分支 merge；从 `FeiMiao` 只移植经审计的文件/逻辑；
- 每 2–4 个紧密页面或一个完整操作链形成单独提交。

### 7.3 验证顺序

1. 纯规则/Store 定向测试；
2. QingJiCore 和 App XCTest；
3. Simulator build；
4. route/fixture/字段 JSON；
5. 原始前后截图和 Android/iOS 对比图；
6. 数据库/文件/网络副作用；
7. 深色、大字体、VoiceOver 和 Reduce Motion 抽查；
8. `git diff --check`、精确 diff、无无关文件。

Windows 没有 Swift/Xcode 时，只能完成静态检查和 Python 门禁；必须等待 macOS CI 后再写“编译/测试通过”。

### 7.4 每批复盘模板

```markdown
# <批次> 复盘

## 完成范围
- 页面/链路：
- 源提交：
- 精确 diff：

## 与 Android 对齐结果
- 入口：通过/失败
- 页面骨架：通过/失败
- 字段顺序：通过/失败
- 业务 JSON：通过/失败
- 正常/空/错误/取消/操作后：

## 证据
- 测试及实际数量：
- Android 原图：
- iOS 改前图：
- iOS 改后图：
- 双端对比图：
- 数据库/文件/网络副作用：

## 允许的平台差异
- 待填写

## 本批发现并已修复的问题
- 待填写

## 本批未关闭缺陷/回归
- 必须为 0；否则本批不得关闭

## 后续计划项（不计作本批缺陷）
- 项目：
- 唯一主责阶段：
- 依赖与验收条件：
- 为什么不属于本批：

## 是否允许进入下一批
- 是/否，理由：
```

### 7.5 防止问题累计的硬规则

- 发现本批回归，先修复再进入下一批；
- 公共组件造成其他页面回归，回归页属于本批范围，不能推给以后；
- 测试失败、错误路由、字段不一致、截图取证不可信时，本批状态只能是 partial；
- “视觉以后统一”“真机以后再看”不能作为模糊豁免；只有阶段表和黄金旅程明确归属后续阶段的事项可以进入“后续计划项”，并且不能被写成已验证；
- 后续阶段修改共享组件或业务规则导致已关闭页面回归时，回归属于当前修改批次，必须当批修复；
- 每批结束都要给出可回退提交，不允许一大批未封板改动横跨多个阶段。

---

## 8. 给 Luna 的直接执行指令

可把下面内容作为 Luna 新任务的开头：

> 你接手的是现有 Android「肥喵记账」的 iOS 同款移植，不是重新设计。先完整读取 `AGENTS.md` 和 `ios-app/docs/IOS_ANDROID_SAME_APP_REBUILD_PLAN_FOR_LUNA.md`。Android 当前生产 App 是唯一产品母版。功能主干使用 `ios-app/QingJi`，不得把 `ios-app/FeiMiao`、`FeiMiaoKit`、GRDB、`com.feimiao.app` 或 `0.2.0` 整套替换为生产实现；`FeiMiao` 分支只能作为首页、抽屉、品牌素材和公共组件的审计性参考。先完成 P0，锁定唯一提交、fixture、逻辑日期、页面身份、metadata、数据升级和分发决策；P0 不负责提前实现 P1–P5 页面，但本阶段证据缺陷未关闭不得进入 UI。之后严格按 P1→P6 推进，每次只做一个小批次，按文档模板复盘；本批缺陷/回归必须为 0，后续计划项必须写明唯一主责阶段和验收条件。禁止 reset/clean/整体 stash/`git add -A`/整分支 merge，不得覆盖用户 Android 改动。Windows 不能声称 Swift/Xcode 通过，必须以 macOS CI 和真机证据为准。

---

## 9. 最终判断标准

完成后，用户不需要理解“SwiftUI 原生适配”才能认出它。打开 iOS 肥喵时，应当立即看到与 Android 相同的品牌、主页、抽屉、记账入口、账单卡、统计层级、预算、资产和 AI 入口；在相同数据下得到相同金额和状态；换成 iPhone 后，只感受到更自然的键盘、触觉、安全区、权限和系统集成，而不是换了一款软件。

最终结论只允许二选一：

- **可以称为真正的 iOS 版**：P0–P6 全部关闭，规范化后的全部页面合同和新增操作状态通过，12 条黄金旅程通过，已有 iOS 数据升级和 Android 备份恢复无丢失，真实 OAuth/扩展/权限在真机运行，选定分发方式真实安装或上传成功且可回退；
- **仍是开发预览版**：上述任一项未验证、用 mock/静态图替代、存在第二数据真相、只出 unsigned IPA、或页面相似但业务副作用不同。
